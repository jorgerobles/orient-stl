# Verification: Phase 3.5 — Scoring Expansion & Refinement

**Date:** 2026-07-13
**Phase:** 03.5
**Status:** ✅ Verified — both sub-plans complete

## Goal-backward check

**Phase goal:** Add the heuristics discovered in the PrusaSlicer comparison
(H5 surface quality, H6 print height), make refine deterministic with a variance
metric (H7), externalise weight profiles to JSON, and add a TOPSIS ranker
with UI switcher.

### Sub-plan 01 — H5/H6 scoring backfill

| Success criterion | Evidence | Pass |
|---|---|---|
| H5 `misalignmentScore` exists in TS + Rust | `web/src/compute.ts:misalignmentScore`, `core/src/scoring.rs:misalignment_score` | ✅ |
| H5 matches PrusaSlicer semantics (maximise, L1 norm in orientation frame) | docstring cites Rotfinder.cpp:88; tests verify face-on minimum + diagonal higher | ✅ |
| H6 `min_z_height` exists in Rust; TS uses existing `computeHeight` | `core/src/scoring.rs:min_z_height`; `web/src/compute.ts:computeHeight` | ✅ |
| `ScoreComponents` carries surface_quality + height | `core/src/scoring.rs` struct + `score_components` populate them | ✅ |
| `Candidate` / `SliceResult` carry `surfaceQuality` / `surface` | interface definitions in `compute.ts` | ✅ |
| `computeSlice` populates `surface` | `compute.ts:computeSlice` calls `misalignmentScore` | ✅ |
| `ScoreWeights` has `wSurface` + `wHeight` | `compute.ts:ScoreWeights` interface | ✅ |
| `WEIGHT_PRESETS` has surface-only + height-only | `compute.ts:WEIGHT_PRESETS` | ✅ |
| `rankByWeights` includes surface (inverted) + height | `compute.ts:rankByWeights` normalises both; tests confirm wHeight/wSurface ordering | ✅ |
| `rankByConsensus` includes all five metrics with equal voice | `compute.ts:rankByConsensus` uses `max(oN, fN, cN, sN, qN, hN)` | ✅ |
| Info tooltip surfaces surface quality | `main.ts` shows `q:` | ✅ |
| Rust harness updated | `harness.rs` `WeightCfg` + configs + `invert_normalise` | ✅ |

### Sub-plan 02 — Seeded refine, JSON profiles, TOPSIS, UI switcher

| Success criterion | Evidence | Pass |
|---|---|---|
| `core/src/rng.rs` provides seeded xorshift32 PRNG with determinism tests | `core/src/rng.rs` with `Rng::new(seed)`, `next_u32()`, `next_f32()`, `next_signed_f32()`, `seed_from_direction()`; 8 tests passing | ✅ |
| `refine_orientation` accepts `seed` param; `refine_orientation_batch` returns K×4 results | `core/src/lib.rs:refine_orientation(..., seed)` (9 args); `refine_orientation_batch(..., k, base_seed)` → K×4 floats | ✅ |
| `computeSlice` calls batch refine (K=4), computes `refinedOverhang` (min) + `refineVariance` (stddev) | `web/src/compute.ts:computeSlice(..., refineFn?)`; K=4 scores extracted at indices 3/7/11/15; `refinedOverhang = Math.min(...scores)`; `refineVariance = sqrt(sum((s-mean)^2)/K)` | ✅ |
| Weight profiles externalised to `web/src/profiles/*.json` with loader | 8 JSON files in `web/src/profiles/`; `profiles/index.ts:loadProfiles()` via `import.meta.glob('./*.json', { eager: true, import: 'default' })` | ✅ |
| `rankByTopsis(candidates, weights)` implements TOPSIS MCDA with tests | `web/src/compute.ts:rankByTopsis` — vector normalisation, weighted Euclidean distance, closeness ∈ [0,1]; 7 tests passing | ✅ |
| UI: profile dropdown + ranker dropdown in `main.ts` | `index.html` has `profile-select` + `ranker-select`; `main.ts` populates profiles dynamically, `applyCurrentRank` switches ranker | ✅ |
| WASM-backed worker with graceful fallback | `orient.worker.ts` imports WASM; wraps `refine_orientation_batch` as `RefineFn`; `try/catch` per direction falls back to raw penalty + 0 variance | ✅ |
| Varita Mágica passes seed param (deterministic) | `main.ts` refine call passes `seed=42` (9 args matching updated WASM signature) | ✅ |

## Quality gates

| Gate | Command | Result |
|------|---------|--------|
| Rust unit tests | `cargo test --lib` (core/) | ✅ 55/55 pass (8 rng + 6 refine spike + 41 pre-existing) |
| WASM rebuild | `wasm-pack build core --target bundler --out-dir web/pkg` | ✅ Success — `refine_orientation_batch` exported |
| TS unit tests | `npm run test` (web/) | ✅ 73/73 pass (16 new: 5 loadProfiles + 7 rankByTopsis + 4 batch refine) |
| Type check | `npm run type-check` (web/) | ✅ 0 errors |
| 8 JSON profiles exist | `ls web/src/profiles/*.json \| wc -l` | ✅ 8 |
| `rankByTopsis` exported | `grep 'export function rankByTopsis' web/src/compute.ts` | ✅ |
| UI dropdowns present | `grep 'profile-select\|ranker-select' web/index.html` | ✅ |
| RefineFn type exported | `grep 'RefineFn' web/src/compute.ts` | ✅ |
| Worker calls batch refine | `grep 'refine_orientation_batch' web/src/orient.worker.ts` | ✅ |

## Deviations from plan

- **[auto-fixed]** `import.meta.glob()` without `import: 'default'` returns module namespace with 6 keys instead of 5 — fixed by adding `import: 'default'` option
- **[auto-fixed]** Missing Vite client types for `import.meta.glob` — fixed by creating `web/src/vite-env.d.ts` with `/// <reference types="vite/client" />`
- **[spike test fix]** `refine_once_different_seeds_may_differ` used dir=[0,0,-1] (already optimal) — fixed by changing to [0,0,1] (non-zero starting score)

## Notes

- Sub-plan 01 (H5/H6) was backfilled: code existed before docs were created.
- Sub-plan 02 executed via GSD: discuss → plan → execute → verify.
- All 12 success criteria from ROADMAP.md are met.
- Phase 3.5 delivers: 5 heuristics, 3 rankers (consensus/weighted/TOPSIS), 8 weight profiles, seeded deterministic refine with batch variance.
