# Plan 03.5-01 SUMMARY: Scoring Expansion

**Phase:** 03.5 (scoring-expansion)
**Plan:** 01
**Status:** Complete (backfilled 2026-07-13)

## What was built

### Task 1: H5 `misalignmentScore` (TS) + `misalignment_score` (Rust)

- **`misalignmentScore(dir, normals, areas)`** in `web/src/compute.ts` — port
  of PrusaSlicer's `get_misalginment_score` (Rotfinder.cpp:88). For each face:
  `area × (|n·dn| + |n·e1| + |n·e2|)` using the `perpendicularBasis(dn)`
  frame. HIGHER = better (maximise metric; rewards faces not aligned to a
  single axis = discourages big flat shelves/walls). O(N).

- **`misalignment_score(direction, mesh)`** in `core/src/scoring.rs` — Rust
  reference parity. Uses the existing `perpendicular_basis` helper.

### Task 2: H6 `min_z_height` (Rust)

- **`min_z_height(direction, mesh)`** in `core/src/scoring.rs` — extent of
  vertices along `direction` (mirrors PrusaSlicer's
  `find_min_z_height_rotation` bbox-Z objective, Rotfinder.cpp:445). LOWER =
  better (fewer layers = faster print). O(N). TS already had `computeHeight`
  (compute.ts:240) producing the same value; this adds Rust parity.

### Task 3: Data model extension

- `Candidate` gained `surfaceQuality: number`
- `SliceResult` gained `surface: number`
- `ScoreComponents` gained `surface_quality: f32` + `height: f32`
- `computeSlice` populates `surface` via `misalignmentScore`
- `mergeCandidates` passes `surfaceQuality` through

### Task 4: Weight config extension

- `ScoreWeights` gained `wSurface: number` + `wHeight: number`
- `WEIGHT_PRESETS` gained `surface-only (finish)` and `height-only (fast)`;
  all existing presets extended with the two new fields

### Task 5: Ranker rewrites

- **`rankByWeights`** now normalises surfaceQuality as cost (inverted:
  `(sMax - val)/sSpan`), height as cost directly, and adds
  `wSurface*sn + wHeight*hn` to the weighted sum.
- **`rankByConsensus`** rewritten: replaced the
  `overhang × (1 + hN×0.5)` multiplicative fudge with height as its own
  independent cost term in the `max()`; surfaceQuality inverted to cost form;
  `1 - max(oN, fN, cN, sN, qN, hN)`. All five heuristics now have equal veto
  power.

### Task 6: UI + harness

- `main.ts` info tooltip shows `q:` (surface quality) readout alongside the
  existing `o f x s H` metrics.
- `harness.rs` `WeightCfg` gained `w_surface` + `w_height`; 8 weight configs
  (added surface-only, height-only, surface/height into resin-biased); new
  `invert_normalise` helper for maximise metrics.

## Verification

| Check | Result |
|-------|--------|
| `cargo test --lib` | ✅ 41/41 tests pass (7 new: 4 misalignment + 3 height) |
| `wasm-pack build core --target bundler --out-dir web/pkg` | ✅ Succeeds (132KB) |
| `npm run test` (vitest) | ✅ 57/57 tests pass (9 new: 4 misalignment + 2 weight + 1 consensus + 2 existing adjusted) |
| `npm run type-check` (tsc --noEmit) | ✅ 0 errors |
| Pre-existing tests unaffected | ✅ All pass |

## Deviations from plan

None — executed as specified. The phase is backfilled: the code landed before
the phase docs were created; this SUMMARY records what was verified at
backfill time.
