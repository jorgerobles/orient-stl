---
phase: 07-correctness-fixes-h11-scoring
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - core/src/candidates.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "compute_default_yaw and its #[deprecated]/#[allow(dead_code)] attributes are deleted from candidates.rs"
    - "The entire dead yaw subgraph is deleted: find_best_yaw, rotate_point, quat_rotate, quat_mul, rotating_calipers_bbox, bbox_area, convex_hull_2d, and test_compute_default_yaw"
    - "The dead `if` branch in rotate_point (recomputing quat_rotate(rot, point) identically) is gone — by deletion of the whole function"
    - "Surviving functions unchanged: generate_candidates, deduplicate_directions, generate_fibonacci_sphere, generate_hull_plus_sphere + their tests"
    - "`use crate::hull::ConvexHull;` is retained (still used by generate_candidates and make_hull test helper)"
    - "`cargo test` passes with no new warnings from the deleted subgraph"
  artifacts:
    - path: "core/src/candidates.rs"
      provides: "candidates module without dead yaw-optimization subgraph"
      contains: "generate_candidates"
  key_links: []
---

<objective>
Delete a dead code subgraph in `core/src/candidates.rs`. `compute_default_yaw` (line 68) is already `#[deprecated]` pointing to `yaw::full_quaternion` / `yaw::bbox_min_yaw` as the replacement, and is invoked only by its own test (`test_compute_default_yaw`). It is not wired into the WASM pipeline.

Deleting it leaves a full chain dead — each function's only callers are within the chain (verified via codegraph):
  compute_default_yaw → find_best_yaw → {rotate_point, quat_mul, rotating_calipers_bbox}
  rotate_point → quat_rotate
  rotating_calipers_bbox → {convex_hull_2d, bbox_area}

Also removes the dead `if` branch in `rotate_point` (line 123, which recomputed `quat_rotate(rot, point)` identically to line 119) — by deleting the whole function.

This is pure hygiene: zero behavioral impact (the deleted code was never called by the pipeline). Reduces dead surface and clears the `#[deprecated]`/`#[allow(dead_code)]` markers.

Output: trimmed candidates.rs, WASM rebuilt.
</objective>

<execution_context>
@/home/jorge/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/jorge/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# The file to trim — dead subgraph spans lines 66-249 + test 282-295
@core/src/candidates.rs

# The replacement that already exists (confirms compute_default_yaw is superseded)
@core/src/yaw.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete the dead yaw subgraph</name>
  <files>
    core/src/candidates.rs
  </files>
  <read_first>
    core/src/candidates.rs,
    core/src/yaw.rs
  </read_first>
  <behavior>
    - Delete (lines approximate, verify by content): compute_default_yaw (66-96 incl. attrs), find_best_yaw (98-116), rotate_point (118-126), quat_rotate (128-144), quat_mul (146-155), rotating_calipers_bbox (157-180), bbox_area (182-197), convex_hull_2d (199-249)
    - Delete test_compute_default_yaw (282-295) from the test module
    - Keep: `use crate::hull::ConvexHull;`, generate_candidates, deduplicate_directions, generate_fibonacci_sphere, generate_hull_plus_sphere, and tests generate_from_hull_normals / dedupe_merges_close_directions / dedupe_keeps_different_directions, plus make_hull helper
    - Confirm via codegraph (or grep) that no other file references the deleted symbols
  </behavior>
  <verification>
    cargo test --manifest-path core/Cargo.toml
    # EXPECT: green; surviving candidate tests pass; no missing-symbol errors
    cargo build --manifest-path core/Cargo.toml
    # EXPECT: no dead_code warnings for the deleted subgraph
    wasm-pack build --target bundler --out-dir ../web/pkg core
  </verification>
</task>

</tasks>
