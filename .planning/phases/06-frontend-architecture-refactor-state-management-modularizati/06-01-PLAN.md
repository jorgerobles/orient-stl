---
phase: 06-frontend-architecture-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/types.ts
  - web/src/compute.ts
  - web/src/constants.ts
  - web/src/centering.ts
  - web/src/centering.test.ts
  - web/src/convention.ts
  - web/src/orient.worker.ts
  - web/src/nearestScore.ts
  - web/src/profiles/index.ts
  - web/src/main.ts
  - web/index.html
  - web/src/styles/theme.css
  - web/src/styles/main.css
autonomous: true
requirements: [C4, C5, C9, C11]
must_haves:
  truths:
    - "compute.ts contains only decimateForScore + WEIGHT_PRESETS const — zero type/interface definitions"
    - "All shared types (OriData, Candidate, ComputeConfig, ScoreWeights) live in types.ts"
    - "WorkerMessage and WorkerRequest discriminated unions exist in types.ts"
    - "Named constants replace magic numbers (DECIMATE_TARGET, STORAGE_KEY, SCHEMA_VERSION, MIN_ANGLE_DEG, etc.)"
    - "liftOntoPlate, SliceResult, RefineFn are deleted with zero remaining references"
    - "index.html contains zero lines of inline CSS (no <style> block)"
  artifacts:
    - path: "web/src/types.ts"
      provides: "All shared types + WorkerMessage/WorkerRequest unions"
      contains: "export type WorkerMessage"
    - path: "web/src/constants.ts"
      provides: "Named constants replacing magic numbers"
      contains: "STORAGE_KEY"
    - path: "web/src/compute.ts"
      provides: "decimateForScore only"
      contains: "export function decimateForScore"
    - path: "web/src/styles/theme.css"
      provides: "CSS custom properties (:root variables)"
      contains: "--color-accent"
    - path: "web/src/styles/main.css"
      provides: "Global layout + scrollbar styles"
      contains: "box-sizing"
  key_links:
    - from: "web/src/compute.ts"
      to: "web/src/types.ts"
      via: "import type OriData"
      pattern: "import.*from.*types"
    - from: "web/src/profiles/index.ts"
      to: "web/src/types.ts"
      via: "import type ScoreWeights"
      pattern: "import.*ScoreWeights.*from.*types"
    - from: "web/src/main.ts"
      to: "web/src/styles/main.css"
      via: "CSS import"
      pattern: "import.*styles/main"
---

<objective>
Foundation layer for the entire Phase 6 refactor. Consolidate all shared types into types.ts, create a constants module for magic numbers, remove three dead exports (liftOntoPlate, SliceResult, RefineFn), and extract the 180-line inline CSS from index.html into a styles/ directory. Every subsequent plan depends on this foundation.

Purpose: Establish the type system and constant vocabulary that AppState (Plan 02), Viewport decomposition (Plan 02), and AppController (Plan 03) will import from. Dead code removal now prevents stale references during later extraction.

Output: Expanded types.ts, stripped compute.ts, new constants.ts, new styles/ directory, cleaned centering.ts, stripped index.html.
</objective>

<execution_context>
@/home/jorge/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/jorge/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-frontend-architecture-refactor-state-management-modularizati/06-RESEARCH.md

# Source files to understand current state (read in read_first blocks, not here)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Consolidate types.ts, create constants.ts, strip compute.ts, remove dead exports</name>
  <files>
    web/src/types.ts,
    web/src/constants.ts,
    web/src/compute.ts,
    web/src/centering.ts,
    web/src/centering.test.ts,
    web/src/convention.ts,
    web/src/orient.worker.ts,
    web/src/nearestScore.ts,
    web/src/profiles/index.ts,
    web/src/main.ts
  </files>
  <read_first>
    web/src/compute.ts,
    web/src/types.ts,
    web/src/centering.ts,
    web/src/centering.test.ts,
    web/src/orient.worker.ts,
    web/src/nearestScore.ts,
    web/src/profiles/index.ts,
    web/src/main.ts,
    web/src/convention.ts
  </read_first>
  <behavior>
    - After refactor, tsc --noEmit passes with zero errors
    - After refactor, vitest run passes with 35 tests (38 original minus 3 liftOntoPlate tests removed)
    - `grep -rn 'SliceResult' web/src/` returns 0 matches
    - `grep -rn 'RefineFn' web/src/` returns 0 matches
    - `grep -rn 'liftOntoPlate' web/src/` returns 0 matches
    - types.ts exports OriData, Candidate, ComputeConfig, ScoreWeights, OrientConfig, WorkerMessage, WorkerRequest
    - compute.ts has zero `export interface` or `export type` statements
  </behavior>
  <action>
    **Step 1 — Expand types.ts:** Move these type definitions from compute.ts into types.ts (which currently holds only OrientConfig + defaultConfig): OriData, Candidate, ComputeConfig, ScoreWeights. Do NOT copy SliceResult or RefineFn — they are dead code and must not appear in types.ts. Then add two new discriminated-union types to types.ts: WorkerMessage (a union of three readonly shapes: {type:'progress', value:number}, {type:'results', candidates:Candidate[]}, {type:'error', message:string}) and WorkerRequest (a single object type with fields: data:OriData, config:ComputeConfig, weights:[number,number,number,number,number], ranker:string, maxCandidates:number, minAngleDeg:number). These unions are defined but NOT yet applied to the worker or handler — that happens in Plan 03 atomically per Pitfall 2.

    **Step 2 — Create web/src/constants.ts:** Extract all magic numbers and string literals into named exports. Key constants: DECIMATE_TARGET=12000 (used in main.ts decimateForScore calls), STORAGE_KEY='orient-stl-config', SCHEMA_VERSION=1, MIN_ANGLE_DEG=15 (worker postMessage param), DEFAULT_REFINE_SEED=42 (score_orientation seed param), DEFAULT_PROFILE='resin-biased', DEFAULT_RANKER='consensus', METRIC_STRIDE=13 (worker metrics layout), MAX_FILE_BYTES=104857600 (100MB, currently in loadSTL.ts as 100*1024*1024). Also include viewport constants: CAMERA_FOV=45, CAMERA_NEAR=0.1, CAMERA_FAR=1000, INITIAL_CAMERA_POS=[30,20,30] as readonly tuple, PLATE_SIZE=60, CAMERA_DIST_MULT=2.5, RING_SCALE=1.3, CAMERA_RING_SCALE=1.6, TUBE_MIN_RATIO=0.006, ANGLE_EPSILON=0.0001.

    **Step 3 — Strip compute.ts:** compute.ts must contain ONLY: an import of OriData type from './types', the WEIGHT_PRESETS const (moved to profiles — see Step 5), and the decimateForScore function. Actually: move WEIGHT_PRESETS to profiles/index.ts (Step 5). compute.ts's final content: `import type { OriData } from './types';` then the decimateForScore function (unchanged logic). Delete the OriData, Candidate, ComputeConfig, SliceResult, RefineFn, ScoreWeights definitions entirely from compute.ts.

    **Step 4 — Move WEIGHT_PRESETS to profiles/index.ts:** profiles/index.ts already calls loadProfiles() and returns the result. Export WEIGHT_PRESETS directly from profiles/index.ts: `import type { ScoreWeights } from '../types'; export const WEIGHT_PRESETS: Record<string, ScoreWeights> = loadProfiles();` Keep the loadProfiles function. Update the import to use types.ts instead of compute.ts.

    **Step 5 — Remove dead code:**
    - Delete liftOntoPlate function from centering.ts (lines 29-31). Keep centroidTranslate and boundingRadius — both are actively used.
    - Delete the entire `describe("liftOntoPlate", ...)` block from centering.test.ts (the import of liftOntoPlate on line 2 must also be removed — keep centroidTranslate and boundingRadius imports).
    - Update the comment in convention.ts line 7 that references "centering.ts → liftOntoPlate" — change to "centering.ts → centroidTranslate" or remove the parenthetical.

    **Step 6 — Update all imports across the codebase:** Every file that currently imports types from './compute' must now import from './types'. Files to update:
    - orient.worker.ts: change `import type { OriData, ComputeConfig, Candidate } from './compute'` to `from './types'`
    - nearestScore.ts: change `import type { Candidate } from './compute'` to `from './types'`
    - main.ts: change `import type { OriData, Candidate, ComputeConfig } from './compute'` to `from './types'`; change `import { decimateForScore, WEIGHT_PRESETS } from './compute'` to split: `import { decimateForScore } from './compute'` and `import { WEIGHT_PRESETS } from './profiles'`. Also replace the literal STORAGE_KEY and SCHEMA_VERSION (lines 229-230) with imports from constants.ts. Replace the literal 12000 in decimateForScore calls with DECIMATE_TARGET. Replace 42 in score_orientation call with DEFAULT_REFINE_SEED. Replace 15 in worker.postMessage with MIN_ANGLE_DEG. Replace 'resin-biased' default with DEFAULT_PROFILE and 'consensus' with DEFAULT_RANKer.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit && npx vitest run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^//' web/src/compute.ts | grep -cE 'export (interface|type)'` returns 0
    - `grep -c 'decimateForScore' web/src/compute.ts` returns at least 1
    - `grep -c 'WEIGHT_PRESETS' web/src/compute.ts` returns 0
    - `grep -rn 'liftOntoPlate\|SliceResult\|RefineFn' web/src/ | grep -v '.test.ts' | wc -l` returns 0 (zero production references)
    - `grep -rn 'liftOntoPlate' web/src/ | wc -l` returns 0 (zero total references including tests)
    - `grep -c 'WorkerMessage' web/src/types.ts` returns at least 1
    - `grep -c 'WorkerRequest' web/src/types.ts` returns at least 1
    - `grep -c 'STORAGE_KEY' web/src/constants.ts` returns at least 1
    - `grep -c 'DECIMATE_TARGET' web/src/constants.ts` returns at least 1
    - npx tsc --noEmit exits 0
    - npx vitest run reports 35 passing tests (38 minus 3 liftOntoPlate tests)
    - npm run build exits 0
  </acceptance_criteria>
  <done>
    compute.ts contains only decimateForScore. All types live in types.ts. constants.ts exists with named constants. liftOntoPlate, SliceResult, RefineFn are deleted. All imports updated. tsc passes. 35 tests pass. Build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract inline CSS from index.html to styles/ directory</name>
  <files>
    web/index.html,
    web/src/styles/theme.css,
    web/src/styles/main.css,
    web/src/main.ts
  </files>
  <read_first>
    web/index.html,
    web/src/main.ts
  </read_first>
  <action>
    **Step 1 — Create web/src/styles/theme.css:** Extract all color/layout values that are reused across components into CSS custom properties on :root. Use the exact values from the current index.html inline styles. Define at minimum: --color-accent (#4a90d9), --color-accent-hover (#357abd), --color-bg (#1a1a1a), --color-viewport-bg (#2a2a2a), --color-panel-bg (rgba(18,18,18,0.92)), --color-text (#eee), --color-text-dim (#888), --color-text-mid (#aaa), --color-success (#27ae60), --color-danger (#e74c3c), --color-warning (#f0ad4e), --color-info (#8cbef0), --radius (10px), --radius-sm (6px), --font-size-base (0.85rem), --border-subtle (1px solid rgba(255,255,255,0.08)).

    **Step 2 — Create web/src/styles/main.css:** Move ALL the remaining CSS rules from index.html's <style> block (lines 9-183) into main.css. Replace hardcoded color values with var(--color-*) references from theme.css where they match the custom properties defined above. Rules to include: universal reset (*), body, #viewport (position, cursor, background), .panel, .panel-left, .panel-right, typography (h1, .subtitle, .status), .drop-zone, .button variants, .progress-* rules, .config-panel, #results, .score-* rules, .sp-* rules, scrollbar rules. Also add a .sr-only class for screen-reader-only content (visually hidden but accessible): position:absolute, width:1px, height:1px, padding:0, margin:-1px, overflow:hidden, clip:rect(0,0,0,0), white-space:nowrap, border:0. This class is needed by Plan 04's accessibility work.

    **Step 3 — Strip the <style> block from index.html:** Remove the entire <style>...</style> element (lines 8-184). The <head> should contain only: <meta charset>, <meta viewport>, <title>, <link rel="icon">. The <body> remains structurally identical for now (semantic HTML landmarks are added in Plan 04).

    **Step 4 — Import CSS in main.ts:** Add two import statements at the top of main.ts (after existing imports): `import './styles/theme.css';` and `import './styles/main.css';`. Vite handles CSS imports natively — no plugin needed. These are side-effect imports (no binding needed).

    Note: Per-component CSS modules (*.module.css) are NOT created in this task. They are created in Plan 03 alongside the view classes that import them. This avoids creating module files with no consumers.
  </action>
  <verify>
    <automated>cd web && npm run build && grep -c '<style' index.html | grep -qx '0' && echo "PASS: no inline style"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '<style' web/index.html` returns 0
    - `grep -c '--color-accent' web/src/styles/theme.css` returns at least 1
    - `grep -c 'box-sizing' web/src/styles/main.css` returns at least 1
    - `grep -c 'sr-only' web/src/styles/main.css` returns at least 1
    - `grep "import.*styles/theme" web/src/main.ts` returns a match
    - `grep "import.*styles/main" web/src/main.ts` returns a match
    - npm run build exits 0 (Vite resolves the CSS imports)
  </acceptance_criteria>
  <done>
    index.html has zero inline CSS. Global styles live in styles/main.css. Theme variables live in styles/theme.css. main.ts imports both. Build succeeds. The app renders identically (CSS values are extracted verbatim).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User file → WASM parser | Binary STL bytes cross from untrusted file input into WASM (unchanged this task) |
| localStorage → app state | Stored config crosses from persistent storage into runtime (unchanged this task) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | localStorage schema (loadConfig/saveConfig) | mitigate | Preserve SCHEMA_VERSION check when moving STORAGE_KEY/SCHEMA_VERSION to constants.ts — the version check logic in loadConfig must remain intact |
| T-06-02 | Information Disclosure | Empty catch blocks (3 instances in main.ts) | accept | These are addressed in Plan 04 Task 2 (criterion 12). This task does not modify catch behavior — it only moves constants. |
| T-06-SC | Tampering | npm install | N/A | Zero packages installed this plan. No package-manager operations. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with 0 errors
- `npx vitest run` passes with 35 tests (3 liftOntoPlate tests removed)
- `npm run build` succeeds
- `grep -rn 'liftOntoPlate\|SliceResult\|RefineFn' web/src/` returns 0 matches
- `grep -c '<style' web/index.html` returns 0
- Zero .rs files touched (WASM rebuild rule does NOT fire)
</verification>

<success_criteria>
- compute.ts contains only decimateForScore (criterion C4)
- All types centralized in types.ts including WorkerMessage/WorkerRequest (criteria C4, C8-type-defs)
- Named constants replace magic numbers (criterion C9)
- liftOntoPlate, SliceResult, RefineFn deleted with zero references (criterion C11)
- Inline CSS extracted to styles/ directory; index.html has no <style> block (criterion C5)
</success_criteria>

<output>
Create `.planning/phases/06-frontend-architecture-refactor-state-management-modularizati/06-01-SUMMARY.md` when done
</output>

## Artifacts This Plan Produces

**New files:**
- `web/src/constants.ts` — named constants (DECIMATE_TARGET, STORAGE_KEY, SCHEMA_VERSION, MIN_ANGLE_DEG, DEFAULT_REFINE_SEED, DEFAULT_PROFILE, DEFAULT_RANKER, METRIC_STRIDE, MAX_FILE_BYTES, CAMERA_FOV, CAMERA_NEAR, CAMERA_FAR, INITIAL_CAMERA_POS, PLATE_SIZE, CAMERA_DIST_MULT, RING_SCALE, CAMERA_RING_SCALE, TUBE_MIN_RATIO, ANGLE_EPSILON)
- `web/src/styles/theme.css` — CSS custom properties (:root variables)
- `web/src/styles/main.css` — global layout/typography/component styles + .sr-only class

**New type symbols in types.ts:**
- `WorkerMessage` — discriminated union (progress | results | error)
- `WorkerRequest` — worker request envelope type

**Modified files:**
- `web/src/types.ts` — gains OriData, Candidate, ComputeConfig, ScoreWeights, WorkerMessage, WorkerRequest
- `web/src/compute.ts` — stripped to decimateForScore only
- `web/src/centering.ts` — liftOntoPlate removed
- `web/src/centering.test.ts` — liftOntoPlate describe block removed
- `web/src/convention.ts` — comment reference updated
- `web/src/orient.worker.ts` — import path updated to types.ts
- `web/src/nearestScore.ts` — import path updated to types.ts
- `web/src/profiles/index.ts` — WEIGHT_PRESETS moved here, import from types.ts
- `web/src/main.ts` — imports updated, constants replace literals, CSS imports added
- `web/index.html` — inline <style> block removed

**Deleted symbols:**
- `liftOntoPlate` (function, was in centering.ts)
- `SliceResult` (interface, was in compute.ts)
- `RefineFn` (type, was in compute.ts)
