---
phase: 06-frontend-architecture-refactor
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - web/src/app/AppState.ts
  - web/src/app/AppState.test.ts
  - web/src/app/constants.ts
  - web/src/viewport/Viewport.ts
  - web/src/viewport/GizmoController.ts
  - web/src/viewport/GizmoController.test.ts
  - web/src/viewport/DragHandler.ts
  - web/src/viewport/DragHandler.test.ts
  - web/src/viewport/CameraRig.ts
  - web/src/viewport/index.ts
autonomous: true
requirements: [C2, C3, C10]
must_haves:
  truths:
    - "AppState notifies subscribers when any field changes via EventTarget"
    - "AppState.unsubscribe stops further notifications"
    - "Viewport delegates ring creation, billboarding, and raycasting to GizmoController"
    - "Viewport delegates pointer drag math to DragHandler"
    - "CameraRig owns camera positioning (positionForBoundingBox + reset)"
    - "Dragging axis-y ring rotates mesh around world Z axis (current behavior preserved — Pitfall 3)"
    - "Viewport's public API (loadModel, showCandidate, setCriticalAngle, enterOverlayMode, getMeshQuaternion, resetCamera, dispose) is unchanged"
    - "main.ts still imports Viewport from './viewport' (barrel resolves)"
  artifacts:
    - path: "web/src/app/AppState.ts"
      provides: "Single mutable state store via EventTarget"
      contains: "extends EventTarget"
    - path: "web/src/viewport/GizmoController.ts"
      provides: "Ring creation, billboarding, raycasting, hover highlight"
      contains: "class GizmoController"
    - path: "web/src/viewport/DragHandler.ts"
      provides: "Pointer capture and angle-delta rotation math"
      contains: "class DragHandler"
    - path: "web/src/viewport/CameraRig.ts"
      provides: "Camera positioning for bounding box + reset"
      contains: "class CameraRig"
    - path: "web/src/viewport/index.ts"
      provides: "Barrel re-export of Viewport"
      contains: "export.*Viewport"
  key_links:
    - from: "web/src/viewport/Viewport.ts"
      to: "web/src/viewport/GizmoController.ts"
      via: "gizmo.billboard(camera) in animate loop"
      pattern: "gizmo.*billboard"
    - from: "web/src/viewport/DragHandler.ts"
      to: "web/src/viewport/GizmoController.ts"
      via: "gizmo.raycastRing for hit detection"
      pattern: "raycastRing"
    - from: "web/src/viewport/Viewport.ts"
      to: "web/src/viewport/CameraRig.ts"
      via: "cameraRig.positionForBoundingBox in loadModel"
      pattern: "cameraRig.*position"
---

<objective>
Create the AppState store (criterion C2) and decompose the 498-line Viewport class into three extracted sub-controllers: GizmoController (ring creation/billboard/raycast/hover), DragHandler (pointer capture/angle math), and CameraRig (camera positioning) (criterion C3). Pin the suspicious axis-mapping behavior with a regression test BEFORE extraction (Pitfall 3). Add unit test coverage for the viewport layer (criterion C10-viewport).

Purpose: AppState is the single source of truth that AppController (Plan 03) will use to replace the 18 module-level let variables. Viewport decomposition makes the gizmo/drag logic independently testable and sets up the structure that AppController's keyboard accessibility (Plan 04) will plug into.

Output: AppState store + tests, viewport/ directory with slimmed Viewport + 3 extracted classes + barrel + tests.
</objective>

<execution_context>
@/home/jorge/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/jorge/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-frontend-architecture-refactor-state-management-modularizati/06-RESEARCH.md
@.planning/phases/06-frontend-architecture-refactor-state-management-modularizati/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create AppState store with EventTarget (TDD)</name>
  <files>
    web/src/app/AppState.ts,
    web/src/app/AppState.test.ts
  </files>
  <read_first>
    web/src/types.ts,
    web/src/constants.ts,
    web/src/main.ts
  </read_first>
  <behavior>
    - Test: subscribing before a set() call receives the callback with correct key and value
    - Test: set('currentProfile', 'topsis') notifies subscribers with key='currentProfile', value='topsis'
    - Test: unsubscribe() return value stops further notifications
    - Test: get('candidates') returns the value previously set
    - Test: set replaces value immutably (does not mutate the original data object)
    - Test: initial values passed to constructor are retrievable via get()
  </behavior>
  <action>
    **RED phase — write tests first in web/src/app/AppState.test.ts:** Import AppState and defaultConfig from respective modules. Create an initial state object with all fields from the AppStateData interface (see below). Write 6 tests covering: subscribe-then-set notification, correct key/value passed to listener, unsubscribe stops notifications, get returns current value, set is immutable (original data not mutated), constructor initial values retrievable. Run tests — they MUST fail (AppState doesn't exist).

    **GREEN phase — create web/src/app/AppState.ts:** Implement AppState as a class extending EventTarget (browser-native, zero deps). The AppStateData interface must contain exactly these 11 fields (this replaces the 18 module-level let vars from main.ts; the 3 redundant vars positions/faceNormals/areas are collapsed into lastOriData per the single-source-of-truth principle): config (OrientConfig), candidates (Candidate[]), currentIndex (number), stlName (string), currentProfile (string), currentRanker (string), isComputeDirty (boolean), lastOriData (OriData | null), liveData ({positions:Float32Array, normals:Float32Array, areas:Float32Array} | null), normBounds ({lo:number[], hi:number[]} | null), loadConvention (LoadConvention). Import LoadConvention from convention.ts, and the other types from types.ts.

    Implement three methods: get(key) returns the current value for that key (generic typed with keyof AppStateData), set(key, value) updates the data immutably (spread into new object, NOT mutation of existing) and dispatches a CustomEvent named 'change' with detail {key, value}, subscribe(listener) adds an event listener and returns an unsubscribe function that removes it. The listener signature is (key: keyof AppStateData, value: unknown) => void. Internally, subscribe wraps the listener to extract key/value from the CustomEvent detail.

    Use the pattern from the research (Pattern 1): dispatchEvent(new CustomEvent('change', { detail: { key, value } })). The subscribe method adds an addEventListener('change', handler) where handler casts the Event to CustomEvent and extracts detail.

    Run tests — they MUST pass now. Commit: test(06-02): add AppState store tests then feat(06-02): implement AppState store.

    **Important constraints:** Do NOT export a singleton instance. The store is always injected via constructor (research Anti-Pattern: "Don't export const state = new AppState() at module scope"). Do NOT include control-flow fields like currentWorker, lastFile, lastFileBytes — those belong to AppController instance state, not observable AppState.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/app/AppState.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'extends EventTarget' web/src/app/AppState.ts` returns 1
    - `grep -c 'dispatchEvent' web/src/app/AppState.ts` returns at least 1
    - `grep -c 'subscribe' web/src/app/AppState.ts` returns at least 1
    - `grep -c 'lastOriData' web/src/app/AppState.ts` returns at least 1
    - `grep -c 'export const state' web/src/app/AppState.ts` returns 0 (no singleton)
    - npx vitest run src/app/AppState.test.ts reports 6 passing tests
    - npx tsc --noEmit passes
  </acceptance_criteria>
  <done>
    AppState store exists with 11 typed fields, EventTarget-based subscribe/notify, immutable set, and 6 passing tests. No singleton export. Ready for AppController to consume in Plan 03.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Full Viewport decomposition — DragHandler regression test first, then extract GizmoController + CameraRig + DragHandler</name>
  <files>
    web/src/viewport/DragHandler.test.ts,
    web/src/viewport/GizmoController.ts,
    web/src/viewport/GizmoController.test.ts,
    web/src/viewport/CameraRig.ts,
    web/src/viewport/DragHandler.ts,
    web/src/viewport/Viewport.ts,
    web/src/viewport/index.ts
  </files>
  <read_first>
    web/src/viewport.ts,
    web/src/constants.ts
  </read_first>
  <behavior>
    - Test (RED-first, written before any extraction): DragHandler.getAxisVector('axis-y') returns [0, 0, 1] (world Z — THIS IS THE CURRENT BEHAVIOR, NOT [0,1,0]. See Pitfall 3.)
    - Test (RED-first): DragHandler.getAxisVector('axis-z') returns [0, 1, 0] (world Y — current behavior)
    - Test (RED-first): DragHandler.getAxisVector('axis-x') returns [1, 0, 0]
    - Test (RED-first): DragHandler.dispose() removes all pointer event listeners (verify with mock addEventListener/removeEventListener)
    - Test (post-extraction): GizmoController.raycastRing returns null when no rings are hit (empty raycaster mock)
    - Test (post-extraction): GizmoController.setHover sets ring material opacity to 1.0 for highlighted ring, restores default on null
    - Test (post-extraction): GizmoController.dispose removes all children from its group
  </behavior>
  <action>
    **RED phase — write DragHandler.test.ts FIRST, before any extraction or Viewport rewrite:** Create web/src/viewport/DragHandler.test.ts with the 4 tests from the behavior block. The axis-mapping tests are the Pitfall 3 regression pin — they document that the current viewport.ts lines 386-396 map: 'axis-x' to Vector3(1,0,0) [correct], 'axis-y' to Vector3(0,0,1) [world Z, NOT Y — suspicious but intentional per research], 'axis-z' to Vector3(0,1,0) [world Y, NOT Z — suspicious but intentional]. The extraction MUST preserve this exact mapping. Do NOT "fix" it. The dispose test verifies cleanup with mock addEventListener/removeEventListener. Run tests — they MUST fail (DragHandler doesn't exist yet). Commit: `test(06-02): add DragHandler axis-mapping regression test (Pitfall 3 pin)`

    **GREEN phase — extract all three sub-controllers and rewrite Viewport in one atomic step:**

    **Step 1 — Delete web/src/viewport.ts and create web/src/viewport/ directory:** The single file must become a directory (TypeScript cannot resolve both viewport.ts and viewport/ simultaneously). Create these files: Viewport.ts (slimmed), GizmoController.ts, CameraRig.ts, DragHandler.ts, index.ts (barrel).

    **Step 2 — Create web/src/viewport/index.ts:** Barrel file that re-exports Viewport: `export { Viewport } from './Viewport';`. This preserves the `import { Viewport } from './viewport'` import path in main.ts.

    **Step 3 — Create web/src/viewport/GizmoController.ts:** Extract from viewport.ts: the makeRing method, createGizmo logic (ring creation with colors 0xff4444/0x44ff44/0x4488ff, tube radii using RING_SCALE and CAMERA_RING_SCALE constants from constants.ts), destroyGizmo, billboardCameraRing, raycastAllRings (renamed to raycastRing taking NDC + camera, returns RingAxis | null), getRingMesh, setHover (hover highlight logic currently inline in onMove), ringDefaultOpacities map. Define and export the RingAxis type: 'axis-x' | 'axis-y' | 'axis-z' | 'camera'. The GizmoController owns its own THREE.Group (readonly property). Constructor takes (modelPosition: THREE.Vector3, boundingRadius: number). Methods: billboard(camera), raycastRing(ndc, camera) returns RingAxis|null, setHover(mode: RingAxis|null), dispose(). Use constants RING_SCALE, CAMERA_RING_SCALE, TUBE_MIN_RATIO from constants.ts for ring geometry.

    **Step 4 — Create web/src/viewport/CameraRig.ts:** Extract camera positioning logic from viewport.ts loadModel (lines 179-188) and resetCamera (lines 211-221). Minimal class with constructor(camera: THREE.PerspectiveCamera, controls: OrbitControls). Methods: positionForBoundingBox(size: THREE.Vector3) — computes maxDim and sets camera position using CAMERA_DIST_MULT constant, resets controls target to origin, calls controls.update(); reset(mesh: THREE.Mesh) — computes bounding box from mesh, calls positionForBoundingBox. Per research: do NOT add animation/easing (YAGNI). Per Pitfall 1: stop at these two methods — do not extract Lights, BuildPlate, or RenderLoop.

    **Step 5 — Create web/src/viewport/DragHandler.ts (GREEN for the RED tests):** Extract from viewport.ts the pointer interaction code: getNDC, intersectRingPlane, angleAroundAxis, the onDown/onMove/onUp handlers from attachPointerHandlers, and the axis-vector mapping logic (lines 385-396). The DragHandler constructor takes: (gizmo: GizmoController, mesh: THREE.Mesh, camera: THREE.PerspectiveCamera, domElement: HTMLCanvasElement, boundingRadius: number, controls: OrbitControls, onOrientationChange: (q:[number,number,number,number]) => void). Expose a public method getAxisVector(mode: RingAxis): [number, number, number] that returns the axis vector for each mode — PRESERVING the current mapping exactly: 'axis-x' returns [1,0,0], 'axis-y' returns [0,0,1], 'axis-z' returns [0,1,0], 'camera' returns the camera's world direction. The onDown handler calls this method instead of using inline if/else. The pointer event listeners (pointerdown, pointermove, pointerup with capture:true) are attached in the constructor and removed in dispose(). The hover-highlight logic (currently in onMove when not dragging) calls gizmo.setHover(mode). The drag logic computes angle deltas exactly as current code does. DragHandler calls onOrientationChange(q) and the Viewport/AppController handles recoloring.

    **CRITICAL — Preserve axis mapping verbatim:** The extraction must copy the axis-vector assignment from viewport.ts lines 385-396 WITHOUT changing which axis maps to which vector. Do not add a comment saying "this looks like a bug" — instead add a comment saying "Intentional: axis-y ring rotates around world Z. See Phase 6 Pitfall 3 regression test. Investigating the mapping is a separate task."

    **Step 6 — Rewrite web/src/viewport/Viewport.ts (slimmed, delegates to all three):** Viewport keeps: scene, camera, renderer, controls, modelGroup, plateGroup, mesh, faceNormals, criticalAngleDeg, animationId, gizmo (GizmoController), cameraRig (CameraRig), dragHandler (DragHandler). Keeps methods: constructor (scene setup, lights, build plate — unchanged), addLights, addBuildPlate, resize, animate (now calls this.gizmo.billboard(this.camera) if gizmo exists), makeColorAttr, colorOverhang, setCriticalAngle, loadModel (now calls this.gizmo = new GizmoController(...), this.cameraRig.positionForBoundingBox(...) instead of inline camera math, and this.dragHandler = new DragHandler(this.gizmo, this.mesh, this.camera, this.renderer.domElement, this.boundingRadius, this.controls, this.onOrientationChange) instead of attachPointerHandlers), showCandidate, applyYaw, resetCamera (now delegates to this.cameraRig.reset(this.mesh)), getMeshQuaternion, enterOverlayMode, exitOverlayMode, get isOverlayActive, dispose (now calls this.gizmo?.dispose(), this.dragHandler?.dispose()). Destroys and recreates GizmoController + DragHandler on loadModel (matching current destroyGizmo + re-attach pattern). Remove the old attachPointerHandlers method entirely. The Viewport's PUBLIC API must remain identical so main.ts does not change.

    **Step 7 — Write GizmoController.test.ts (post-extraction):** Mock THREE.Raycaster, THREE.Vector3, THREE.TorusGeometry as needed. Test raycastRing returns null on no hit, setHover toggles opacity, dispose clears the group. Use vitest vi.fn() and manual mock objects — do NOT import real three.js (keep node environment).

    Run all tests + tsc + build. The DragHandler regression tests (Pitfall 3) MUST pass — if they fail, the extraction changed the mapping, fix the extraction not the test. The app must render identically (same gizmo rings, same camera position, same drag behavior). Commit: `feat(06-02): decompose Viewport into GizmoController + CameraRig + DragHandler`
  </action>
  <verify>
    <automated>cd web && npx vitest run src/viewport/ && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `ls web/src/viewport.ts 2>/dev/null` fails (file deleted)
    - `ls web/src/viewport/Viewport.ts` succeeds (directory created)
    - `grep -c 'class GizmoController' web/src/viewport/GizmoController.ts` returns 1
    - `grep -c 'class CameraRig' web/src/viewport/CameraRig.ts` returns 1
    - `grep -c 'class DragHandler' web/src/viewport/DragHandler.ts` returns 1
    - `grep -c 'getAxisVector' web/src/viewport/DragHandler.ts` returns at least 1
    - `grep -c '0, 0, 1' web/src/viewport/DragHandler.ts` returns at least 1 (axis-y maps to world Z)
    - `grep -c '0, 1, 0' web/src/viewport/DragHandler.ts` returns at least 1 (axis-z maps to world Y)
    - `grep -c 'Pitfall 3' web/src/viewport/DragHandler.ts` returns at least 1 (comment documenting the pin)
    - `grep -c 'billboard' web/src/viewport/GizmoController.ts` returns at least 1
    - `grep -c 'raycastRing' web/src/viewport/GizmoController.ts` returns at least 1
    - `grep -c 'positionForBoundingBox' web/src/viewport/CameraRig.ts` returns at least 1
    - `grep -c 'export.*Viewport' web/src/viewport/index.ts` returns at least 1
    - `grep -c 'GizmoController' web/src/viewport/Viewport.ts` returns at least 1 (Viewport uses GizmoController)
    - `grep -c 'CameraRig' web/src/viewport/Viewport.ts` returns at least 1 (Viewport uses CameraRig)
    - `grep -c 'DragHandler' web/src/viewport/Viewport.ts` returns at least 1 (Viewport uses DragHandler)
    - `grep -c 'attachPointerHandlers' web/src/viewport/Viewport.ts` returns 0 (old method removed)
    - npx vitest run src/viewport/ reports all DragHandler + GizmoController tests passing
    - npx tsc --noEmit passes
    - npm run build succeeds (barrel resolves, app compiles)
  </acceptance_criteria>
  <done>
    viewport.ts deleted. viewport/ directory contains slimmed Viewport.ts, GizmoController.ts, DragHandler.ts, CameraRig.ts, and index.ts barrel. DragHandler.test.ts written FIRST as RED regression test (Pitfall 3 axis-mapping pinned: axis-y → world Z, axis-z → world Y). All three sub-controllers extracted atomically in one GREEN step. Viewport delegates to all three; attachPointerHandlers removed. Viewport public API unchanged. All tests pass (DragHandler 4 + GizmoController 3). Build succeeds.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pointer events → mesh rotation | User pointer input crosses into THREE.Quaternion mutation via DragHandler |
| AppState → view subscriptions | State mutations cross into view re-render via EventTarget dispatch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | AppState subscribe/unsubscribe lifecycle | mitigate | Each view's dispose() must call the unsubscribe function returned by state.subscribe(). AppController (Plan 03) tracks all views and disposes on file reload. Pitfall 4. |
| T-06-04 | Repudiation | DragHandler axis-mapping silent behavior change | mitigate | Pitfall 3 regression test pins axis-y → world Z mapping. Any change breaks the test explicitly. |
| T-06-05 | Denial of Service | DragHandler pointer listeners not cleaned up | mitigate | DragHandler.dispose() removes all listeners. Viewport.dispose calls dragHandler.dispose(). |
| T-06-SC | Tampering | npm install | N/A | Zero packages installed this plan. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with 0 errors
- `npx vitest run` passes all tests (existing 35 + new AppState 6 + GizmoController tests + DragHandler 4)
- `npm run build` succeeds
- `ls web/src/viewport.ts` fails (single file replaced by directory)
- `ls web/src/viewport/` shows Viewport.ts, GizmoController.ts, DragHandler.ts, CameraRig.ts, index.ts
- App renders identically when loaded in browser (manual smoke check: load STL, drag rings, navigate candidates)
- Zero .rs files touched
</verification>

<success_criteria>
- AppState store exists with 11 fields and subscribe/notify/unsubscribe (criterion C2)
- Viewport split into GizmoController + DragHandler + CameraRig (criterion C3)
- GizmoController and DragHandler have unit test coverage (criterion C10-viewport)
- Axis-mapping behavior pinned by regression test (Pitfall 3 mitigated)
- Viewport public API unchanged (main.ts import still works via barrel)
</success_criteria>

<output>
Create `.planning/phases/06-frontend-architecture-refactor-state-management-modularizati/06-02-SUMMARY.md` when done
</output>

## Artifacts This Plan Produces

**New files:**
- `web/src/app/AppState.ts` — AppState class (extends EventTarget)
- `web/src/app/AppState.test.ts` — 6 unit tests
- `web/src/viewport/Viewport.ts` — slimmed Viewport (moved from viewport.ts)
- `web/src/viewport/GizmoController.ts` — GizmoController class + RingAxis type
- `web/src/viewport/GizmoController.test.ts` — GizmoController unit tests
- `web/src/viewport/DragHandler.ts` — DragHandler class
- `web/src/viewport/DragHandler.test.ts` — 4 unit tests (including Pitfall 3 regression)
- `web/src/viewport/CameraRig.ts` — CameraRig class
- `web/src/viewport/index.ts` — barrel re-export

**Deleted files:**
- `web/src/viewport.ts` (replaced by viewport/ directory)

**New symbols:**
- `AppState` (class) — single mutable state store
- `GizmoController` (class) — ring creation, billboard, raycast, hover
- `RingAxis` (type) — 'axis-x' | 'axis-y' | 'axis-z' | 'camera'
- `DragHandler` (class) — pointer capture + angle-delta rotation math
- `CameraRig` (class) — camera positioning (positionForBoundingBox, reset)
