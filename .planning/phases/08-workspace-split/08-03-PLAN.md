---
phase: 08-workspace-split
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - core/ (deleted)
  - web/src/types.ts
  - web/src/compute.ts
  - web/src/main.ts
  - web/src/views/ScorePanel.ts
  - web/src/app/AppController.ts
autonomous: false
requirements: []
must_haves:
  truths:
    - "App loads STL, scores, ranks, displays candidates — identical to pre-split behavior"
    - "CLI binary (`cargo run --bin orient`) works with new crate structure"
    - "No references to old `core/` crate remain in web/ or Cargo.toml"
    - "All TypeScript tests pass"
    - "All Rust tests pass"
  artifacts:
    - path: "web/src/types.ts"
      provides: "Updated imports for new WASM modules"
      contains: "pipeline"
    - path: "Makefile"
      provides: "Complete build system"
      contains: "wasm-orient"
  key_links:
    - from: "web/src/main.ts"
      to: "web/src/pipeline.ts"
      via: "import runPipeline"
      pattern: "runPipeline"
    - from: "crates/orient/src/main.rs"
      to: "crates/orient/src/lib.rs"
      via: "use orient_core::"
      pattern: "orient_core"
---

<objective>
Verify the workspace split is complete and correct. Remove old `core/` directory. Fix any remaining type/import issues. Run full test suite to confirm behavioral equivalence.

Purpose: Ensure the restructure introduces zero regressions before moving to new feature development (support module).

Output: Clean workspace with no old artifacts. All tests passing. CLI binary working.
</objective>

<execution_context>
@/home/r2d2/.config/opencode/get-shit-done/workflows/execute-plan.md
@home/r2d2/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/HANDOFF.md
@.planning/phases/08-workspace-split/08-01-SUMMARY.md
@.planning/phases/08-workspace-split/08-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete old core/ + fix imports + verify CLI</name>
  <files>
    core/ (deleted entirely),
    web/src/types.ts,
    crates/orient/src/main.rs
  </files>
  <action>
1. **Delete `core/` directory entirely**: `rm -rf core/`

2. **Fix any remaining imports in web/src/**: Search for references to `../pkg/orient_core` or `orient_core` in web/src/ files. Replace with new module paths:
   - `loadSTL.ts`: should import from pipeline.ts, not directly from pkg/orient_core
   - `orient.worker.ts`: should import from `../pkg/orient_js.js` (or whatever wasm-pack names the output)
   - `types.ts`: Update `OriData` import if it moved to a different module

3. **Verify CLI binary**: `cargo run --bin orient -- --help` should work. The CLI is in `crates/orient/src/main.rs`. Ensure it imports from `orient_core` (the crate name) and all dependencies resolve.

4. **Update web/src/types.ts**: If `OriData` or `Candidate` types need to reference the new WASM output format, update them. The flat f32 array format should be identical — only the import paths change.

5. **Search for orphan references**: `grep -r "orient.core\|orient_core\|core/src" web/ crates/ --include="*.ts" --include="*.rs" --include="*.toml"` should return nothing (except legitimate crate names).
  </action>
  <verify>
    <automated>cd /home/r2d2/Sandbox/orient-stl && test ! -d core && echo "core/ deleted" && cargo run --bin orient -- --help 2>&1 | head -5</automated>
  </verify>
  <done>core/ directory deleted, CLI binary works, no orphan references to old structure</done>
</task>

<task type="auto">
  <name>Task 2: Full test suite verification</name>
  <files>
    web/src/app/AppController.test.ts,
    web/src/views/ScorePanel.test.ts,
    web/src/viewport/GizmoController.test.ts
  </files>
  <action>
Run the complete test suites and fix any failures caused by the restructure:

1. **Rust tests**: `cargo test --workspace` — all 96+ unit tests and integration tests must pass
2. **TypeScript tests**: `cd web && npm run test` — all 38+ tests must pass
3. **Type check**: `cd web && npx tsc --noEmit` — clean
4. **Build**: `cd web && npm run build` — Vite build succeeds

If any test fails, fix the import path or type reference. Common issues:
- Test files importing from old `../pkg/orient_core` paths
- Mock worker setup referencing old worker structure
- Types that reference moved modules

Also verify the CLI produces correct output:
```bash
cargo run --bin orient -- path/to/test.stl --decimate 12000 2>&1 | head -20
```
  </action>
  <verify>
    <automated>cd /home/r2d2/Sandbox/orient-stl && cargo test --workspace 2>&1 | grep -E "test result|FAILED" && cd web && npm run test 2>&1 | grep -E "Tests|FAIL"</automated>
  </verify>
  <done>All Rust tests pass, all TypeScript tests pass, tsc clean, Vite build succeeds, CLI produces valid output</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Final verification — behavioral equivalence</name>
  <what-built>
Complete workspace split: 5 independent crates, 4 WASM binaries, pipeline.ts orchestrator, CLI binary. All tests passing.
  </what-built>
  <how-to-verify>
1. Run `make wasm && cd web && npm run dev`
2. Load a test STL file (e.g., the same one used before the split)
3. Verify: candidates load, navigation works, yaw adjustment works, export works
4. Verify: scoring results are identical to pre-split (same candidates, same scores)
5. Run `cargo run --bin orient -- test.stl` — verify CLI output matches browser results
6. Run `make test && make type-check` — green
7. Check bundle size: `ls -la web/dist/assets/` — WASM binaries should be ~50-200KB each
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WASM binaries ↔ browser | Each binary loads independently; no cross-binary trust assumptions |
| CLI ↔ filesystem | CLI reads STL files directly; no WASM boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Elevation of Privilege | Old core/ references | mitigate | grep verification ensures no stale imports remain |
| T-08-07 | Denial of Service | Multiple WASM loads | accept | 4 WASM binaries × ~100KB each = ~400KB total; parallel loading via workers |
</threat_model>

<verification>
1. `cargo test --workspace` — all pass
2. `cd web && npm run test` — all pass
3. `cd web && npx tsc --noEmit` — clean
4. `make wasm` — all binaries build
5. Manual: app loads STL, scores, navigates, exports — identical to pre-split
6. `core/` directory does not exist
</verification>

<success_criteria>
- core/ deleted
- All tests passing (Rust + TypeScript)
- CLI binary works
- App behavior identical to pre-split
- No orphan imports or references
</success_criteria>

<output>
Create `.planning/phases/08-workspace-split/08-03-SUMMARY.md` when done
</output>
