---
phase: 08-workspace-split
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/stl-parse/Cargo.toml
  - crates/stl-parse/src/lib.rs
  - crates/stl-repair/Cargo.toml
  - crates/stl-repair/src/lib.rs
  - crates/mesher/Cargo.toml
  - crates/mesher/src/lib.rs
  - crates/orient/Cargo.toml
  - crates/orient/src/lib.rs
  - crates/orient/src/scoring.rs
  - crates/orient/src/ranking.rs
  - crates/orient/src/selection.rs
  - crates/orient/src/hull.rs
  - crates/orient/src/candidates.rs
  - crates/orient/src/stability.rs
  - crates/orient/src/yaw.rs
  - crates/orient/src/rng.rs
  - crates/orient/src/decimate.rs
  - crates/orient/src/main.rs
  - crates/geometry-kernel/Cargo.toml
  - Cargo.toml (workspace root)
autonomous: true
requirements: []
must_haves:
  truths:
    - "Each crate compiles independently as cdylib (WASM) or rlib (shared)"
    - "Workspace cargo build succeeds for all crates"
    - "All existing Rust tests pass without modification"
    - "geometry-kernel is rlib-only (no cdylib, no WASM bindings)"
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace root with member paths"
      contains: "members"
    - path: "crates/stl-parse/src/lib.rs"
      provides: "STL binary parsing"
      exports: ["parse_stl_bytes"]
    - path: "crates/stl-repair/src/lib.rs"
      provides: "Mesh repair pipeline"
      exports: ["repair_positions"]
    - path: "crates/mesher/src/lib.rs"
      provides: "Mesh precomputation"
      exports: ["precompute_mesh_data"]
    - path: "crates/orient/src/lib.rs"
      provides: "Orientation scoring + ranking + selection"
      exports: ["score_all_directions", "rank_candidates", "select_diverse"]
    - path: "crates/geometry-kernel/Cargo.toml"
      provides: "Shared geometry library"
      contains: 'crate-type = ["rlib"]'
  key_links:
    - from: "crates/stl-repair/src/lib.rs"
      to: "crates/geometry-kernel"
      via: "Cargo.toml dependency"
      pattern: "geometry-kernel"
    - from: "crates/orient/src/lib.rs"
      to: "crates/geometry-kernel"
      via: "Cargo.toml dependency"
      pattern: "geometry-kernel"
---

<objective>
Split the coupled `core/` monolith into independent crates under `crates/`, each with a single responsibility. Create a Cargo workspace that builds all crates. Move existing Rust source files into the correct crate boundaries. Convert geometry-kernel to rlib-only.

Purpose: Establish the Unix-style WASM module architecture where each crate is an independent compilation unit. This is the foundation for separate WASM binaries and independent loading.

Output: 5 independent crates (stl-parse, stl-repair, mesher, orient, geometry-kernel) + workspace Cargo.toml. All `cargo test` passes.
</objective>

<execution_context>
@/home/r2d2/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/r2d2/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/HANDOFF.md
@.planning/research/SUPPORT-GENERATION.md
@.planning/research/ARCHITECTURE.md

<interfaces>
<!-- Key types and contracts the executor needs from existing codebase. -->

From core/src/lib.rs:
- `prepare_data_native(bytes, mode, dedupe_angle_deg) -> Result<OriData, String>` — full pipeline
- `OriData { positions, normals, areas, directions }` — flat f32 arrays
- `reconstruct_mesh(positions, normals, areas) -> MeshData` — shared helper
- `normalise_dir(d) -> ([f32;3], f32)` — shared helper
- `refine_once(mesh, start_dir, critical_angle_deg, iterations, rng) -> ([f32;3], f32)` — hill-climb

From core/src/stl.rs:
- `parse_stl(bytes: &[u8]) -> Result<Vec<[f32; 3]>, String>` — binary STL parser

From core/src/mesh.rs:
- `precompute_mesh(positions: &[f32]) -> MeshData` — normals, areas, vertices
- `MeshData { normals, areas, vertices, triangle_count }` — core data structure

From core/src/scoring.rs:
- `score_components(dir, mesh, critical_angle_deg, samples) -> ScoreComponents`
- `score_candidate(dir, mesh, critical_angle_deg) -> f32`
- `perpendicular_basis(dir) -> ([f32;3], [f32;3])`

From core/src/ranking.rs:
- `rank_by_weights_with_bounds`, `rank_by_consensus_with_bounds`, `rank_by_topsis`
- `ScoreWeights`, `CandidateMetrics`

From core/src/selection.rs:
- `merge_candidates(scored, dirs, stable, exclude_unstable, max_candidates, min_angle_deg) -> Vec<usize>`

From core/src/hull.rs:
- `compute_hull(vertices) -> Hull` — convex hull
- `Hull { face_normals, vertices }`

From core/src/candidates.rs:
- `generate_candidates(hull) -> Vec<[f32;3]>`
- `generate_hull_plus_sphere(hull, samples, dedupe_angle) -> Vec<[f32;3]>`
- `deduplicate_directions(directions, angle_deg) -> Vec<[f32;3]>`

From core/src/stability.rs:
- `check_stability(dir, mesh) -> StabilityResult`

From core/src/yaw.rs:
- `full_quaternion(dir, mesh) -> [f32;4]`

From core/src/rng.rs:
- `Rng::new(seed)`, `seed_from_direction(dir, base_seed)`

From core/src/decimate.rs:
- `sample_for_hull(vertices) -> Vec<[f32;3]>`

From geometry-kernel/src/flat.rs:
- `repair_mesh(positions: &mut Vec<f32>)`
- `normalize_winding(positions: &mut Vec<f32>)`
- `weld_vertices(positions: &mut Vec<f32>, epsilon: f32)`
- `fill_holes(positions: &mut Vec<f32>, max_edges: u32)`
- `count_boundary_edges(positions: &[f32]) -> u32`
- `DEFAULT_WELD_EPSILON: f32`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create workspace root + crate scaffolding</name>
  <files>
    Cargo.toml,
    crates/stl-parse/Cargo.toml, crates/stl-parse/src/lib.rs,
    crates/stl-repair/Cargo.toml, crates/stl-repair/src/lib.rs,
    crates/mesher/Cargo.toml, crates/mesher/src/lib.rs,
    crates/orient/Cargo.toml, crates/orient/src/lib.rs,
    crates/geometry-kernel/Cargo.toml, crates/geometry-kernel/src/lib.rs
  </files>
  <action>
Create the workspace root `Cargo.toml` at project root with members:
`["crates/stl-parse", "crates/stl-repair", "crates/mesher", "crates/orient", "crates/geometry-kernel"]`.

For each crate, create `Cargo.toml` with:
- `stl-parse`: cdylib + rlib, deps = `stl_io = "0.11"`, `serde`, `wasm-bindgen` (optional wasm feature)
- `stl-repair`: cdylib + rlib, deps = `geometry-kernel = { path = "../geometry-kernel" }`, `wasm-bindgen` (optional)
- `mesher`: cdylib + rlib, deps = `serde`, `wasm-bindgen` (optional)
- `orient`: cdylib + rlib + bin (orient CLI), deps = `geometry-kernel`, `serde`, `stl_io`, `wasm-bindgen` (optional), `clap` (optional cli feature), `serde_json` (optional cli)
- `geometry-kernel`: **rlib only** (remove cdylib), deps = `wasm-bindgen`, `serde`, `serde-wasm-bindgen`, `js-sys`, `log`, `console_error_panic_hook`, `rayon` (native only). **Remove all `#[wasm_bindgen]` exports** from geometry-kernel — it becomes a pure rlib.

Each crate gets a minimal `src/lib.rs` stub that re-exports the modules it will contain. Use feature gates: `default = ["wasm"]`, `wasm = ["wasm-bindgen", ...]`, `cli = ["serde_json", "clap"]`.

For `geometry-kernel`, change `crate-type = ["rlib"]` (remove `"cdylib"`). Remove all `#[wasm_bindgen]` functions from its source — the WASM exports that were in `geometry-kernel/src/flat.rs` are not needed (orient-core calls the rlib functions directly).

Create directory structure:
```
mkdir -p crates/{stl-parse,stl-repair,mesher,orient,geometry-kernel}/src
```
  </action>
  <verify>
    <automated>cd /home/r2d2/Sandbox/orient-stl && cargo check --workspace 2>&1 | tail -5</automated>
  </verify>
  <done>Workspace Cargo.toml exists, all 5 crates have Cargo.toml + stub lib.rs, `cargo check --workspace` compiles (even if with warnings about unused stubs)</done>
</task>

<task type="auto">
  <name>Task 2: Move source files into correct crates + wire dependencies</name>
  <files>
    crates/stl-parse/src/lib.rs,
    crates/stl-repair/src/lib.rs,
    crates/mesher/src/lib.rs,
    crates/orient/src/lib.rs,
    crates/orient/src/scoring.rs,
    crates/orient/src/ranking.rs,
    crates/orient/src/selection.rs,
    crates/orient/src/hull.rs,
    crates/orient/src/candidates.rs,
    crates/orient/src/stability.rs,
    crates/orient/src/yaw.rs,
    crates/orient/src/rng.rs,
    crates/orient/src/decimate.rs,
    crates/orient/src/main.rs,
    crates/geometry-kernel/src/lib.rs,
    crates/geometry-kernel/src/flat.rs,
    crates/geometry-kernel/src/types.rs,
    crates/geometry-kernel/src/mesh.rs,
    crates/geometry-kernel/src/repair.rs,
    crates/geometry-kernel/src/analysis.rs
  </files>
  <action>
Move source files from `core/src/` to their target crates:

1. **stl-parse**: Move `core/src/stl.rs` → `crates/stl-parse/src/lib.rs`. The function `parse_stl(bytes: &[u8]) -> Result<Vec<[f32;3]>, String>` becomes the crate's public API. Add `pub fn parse_stl_bytes(bytes: &[u8]) -> Result<Vec<f32>, String>` that returns flat f32 positions (9 floats per triangle) for WASM consumption. Keep the existing `parse_stl` as internal.

2. **stl-repair**: Create `crates/stl-repair/src/lib.rs` that re-exports geometry-kernel flat operations as a coherent repair pipeline:
   ```rust
   pub fn repair_positions(positions: &mut Vec<f32>, weld_epsilon: f32, max_hole_edges: u32) {
       geometry_kernel::flat::repair_mesh(positions);
       geometry_kernel::flat::normalize_winding(positions);
       if weld_epsilon > 0.0 {
           geometry_kernel::flat::weld_vertices(positions, weld_epsilon);
           geometry_kernel::flat::repair_mesh(positions);
       }
       if max_hole_edges > 0 {
           geometry_kernel::flat::fill_holes(positions, max_hole_edges);
       }
   }
   ```
   Add WASM binding: `pub fn repair_mesh_wasm(positions: Vec<f32>, weld_epsilon: f32, max_hole_edges: u32) -> Vec<f32>` gated behind `#[cfg(feature = "wasm")]`.

3. **mesher**: Create `crates/mesher/src/lib.rs` that wraps `geometry_kernel::mesh::precompute_mesh`:
   ```rust
   pub struct MeshOutput { pub positions: Vec<f32>, pub normals: Vec<f32>, pub areas: Vec<f32> }
   pub fn precompute_mesh_data(positions: &[f32]) -> Result<MeshOutput, String> { ... }
   ```
   The function calls `mesh::precompute_mesh`, filters degenerate triangles, and returns flat f32 arrays.

4. **orient**: Move these files from `core/src/`:
   - `scoring.rs` → `crates/orient/src/scoring.rs`
   - `ranking.rs` → `crates/orient/src/ranking.rs`
   - `selection.rs` → `crates/orient/src/selection.rs`
   - `hull.rs` → `crates/orient/src/hull.rs`
   - `candidates.rs` → `crates/orient/src/candidates.rs`
   - `stability.rs` → `crates/orient/src/stability.rs`
   - `yaw.rs` → `crates/orient/src/yaw.rs`
   - `rng.rs` → `crates/orient/src/rng.rs`
   - `decimate.rs` → `crates/orient/src/decimate.rs`
   - `main.rs` → `crates/orient/src/main.rs`

   The `crates/orient/src/lib.rs` gets the WASM exports (`score_all_directions`, `rank_candidates`, `select_diverse`, `compute_norm_bounds`, `score_direction`, `refine_orientation`, `refine_orientation_batch`, `score_orientation`) and shared helpers (`reconstruct_mesh`, `normalise_dir`, `refine_once`, `tangent_perturbation`). Also keep `prepare_data_native` and `prepare_data_native_with_repair` as the combined pipeline entry points (these orchestrate stl-parse + repair + mesher + orient internally via crate dependencies).

   **Key**: `orient` depends on `stl-parse`, `stl-repair`, `mesher`, and `geometry-kernel`. The `prepare_data_native` functions stay in orient since they orchestrate the full pipeline. Individual WASM exports for each module go in their respective crates.

5. **geometry-kernel**: Copy `core/src/` files that are shared mesh ops. Actually geometry-kernel already has its own `src/`. The key change is:
   - Remove `crate-type = ["cdylib"]` from Cargo.toml (keep only `["rlib"]`)
   - Remove ALL `#[wasm_bindgen]` exports from geometry-kernel source
   - Remove WASM-specific dependencies (`wasm-bindgen`, `serde-wasm-bindgen`, `js-sys`) — make them optional behind a feature that orient/stl-repair enable
   - Keep `repair_mesh`, `normalize_winding`, `weld_vertices`, `fill_holes`, `count_boundary_edges`, `DEFAULT_WELD_EPSILON` as public rlib API

Update all `Cargo.toml` dependency paths to point to `../geometry-kernel`.

After moving, delete the old `core/` directory.

**IMPORTANT**: Keep the `core/src/harness.rs` test helpers — move to `crates/orient/src/harness.rs` (it's `#[cfg(test)]` only).
  </action>
  <verify>
    <automated>cd /home/r2d2/Sandbox/orient-stl && cargo test --workspace 2>&1 | tail -20</automated>
  </verify>
  <done>All existing Rust tests pass. Each crate compiles independently. `cargo test --workspace` shows same test count as before the split. No `core/` directory remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WASM modules ↔ JS orchestrator | Each WASM binary exposes a narrow API via wasm-bindgen; flat f32 arrays cross the boundary |
| crate ↔ crate | rlib dependencies; no WASM boundary crossing between crates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | Cargo.toml workspace | mitigate | Lock dependency versions; verify `cargo audit` passes after restructure |
| T-08-02 | Information Disclosure | geometry-kernel rlib | accept | rlib is internal-only, not exposed to WASM boundary |
| T-08-03 | Elevation of Privilege | wasm-pack build per crate | mitigate | Each crate builds independently; verify no crate imports unauthorized dependencies |
</threat_model>

<verification>
1. `cargo check --workspace` — all crates compile
2. `cargo test --workspace` — all tests pass (same count as before split)
3. `cargo build --release -p orient-core` — WASM binary builds
4. Each crate's `Cargo.toml` has correct `crate-type` (geometry-kernel = rlib only, others = cdylib + rlib)
5. No `core/` directory remains
</verification>

<success_criteria>
- 5 independent crates under `crates/`, each with correct Cargo.toml
- Workspace root Cargo.toml builds all crates
- All existing tests pass
- geometry-kernel is rlib-only with no WASM bindings
- `core/` directory deleted
</success_criteria>

<output>
Create `.planning/phases/08-workspace-split/08-01-SUMMARY.md` when done
</output>
