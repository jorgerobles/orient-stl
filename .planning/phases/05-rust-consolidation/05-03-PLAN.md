---
phase: 05-rust-consolidation
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - web/src/compute.ts
  - web/src/compute.test.ts
  - web/src/orient.worker.ts
  - web/src/main.ts
  - web/src/profiles/index.ts
  - web/src/nearestScore.ts
autonomous: true
requirements: []
tags: [typescript, thin-layer, worker, deletion, wasm-dispatcher]

must_haves:
  truths:
    - "web/src/compute.ts contains ONLY: type definitions (OriData, Candidate, ComputeConfig, SliceResult, ScoreWeights, RefineFn) and the WEIGHT_PRESETS import"
    - "web/src/compute.test.ts does NOT exist (deleted per D-03)"
    - "No metric/ranking/selection/yaw/geometry function exists in TypeScript"
    - "The worker calls wasm.score_all_directions (and optionally wasm.rank_candidates) in a single WASM dispatch — no multi-worker computeSlice coordination"
    - "main.ts calls the worker for scoring + WASM rank_candidates (via worker) for ranking — no longer calls TS rankByX functions"
    - "Browser builds: `npm run type-check`, `npm run build`, `npm test` all pass"
  artifacts:
    - path: "web/src/compute.ts"
      provides: "Type definitions only for WASM return shapes"
      contains: "export interface OriData"
    - path: "web/src/nearestScore.ts"
      provides: "nearestCandidateScore display helper (rendering-layer)"
      contains: "export function nearestCandidateScore"
    - path: "web/src/orient.worker.ts"
      provides: "Thin WASM dispatcher — calls score_all_directions and posts back results"
      contains: "score_all_directions"
  key_links:
    - from: "web/src/orient.worker.ts"
      to: "wasm.score_all_directions (in web/pkg)"
      via: "single WASM call"
      pattern: "score_all_directions"
    - from: "web/src/main.ts"
      to: "web/src/orient.worker.ts (single worker)"
      via: "postMessage / onmessage"
      pattern: "new Worker"
    - from: "web/src/profiles/index.ts"
      to: "web/src/compute.ts::ScoreWeights"
      via: "type import after refactor"
      pattern: "ScoreWeights"
---

<objective>
Strip `web/src/compute.ts` down to type definitions only, delete `web/src/compute.test.ts` entirely, simplify the worker to a single-call WASM dispatcher (per D-01), and update `main.ts` to call WASM scoring/ranking through the thin worker — eliminating the multi-worker `computeSlice` pipeline and all TS metric/ranking/selection/yaw code. Per D-03 (delete TS metric/ranking tests), D-06 (TS thin layer locked).

Purpose: Achieve the single source of truth goal — TS becomes rendering-only, all calculation lives in Rust.
Output: Stripped compute.ts (types only), deleted compute.test.ts, simplified worker, updated main.ts, passing browser builds + tests.
</objective>

<execution_context>
@/home/jorge/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/jorge/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-rust-consolidation/05-CONTEXT.md
@.planning/phases/05-rust-consolidation/05-RESEARCH.md
@.planning/phases/05-rust-consolidation/05-01-SUMMARY.md
@.planning/phases/05-rust-consolidation/05-02-SUMMARY.md

# Source files the executor MUST read before implementation
@web/src/compute.ts
@web/src/compute.test.ts
@web/src/orient.worker.ts
@web/src/main.ts
@web/src/profiles/index.ts
@web/src/types.ts
@web/src/quaternion.ts
@web/src/rotate.ts
@web/src/convention.ts
@web/src/centering.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip compute.ts to type definitions only + extract nearestCandidateScore</name>
  <read_first>
    - web/src/compute.ts (full file — to know exactly what to delete and what to keep)
    - web/src/compute.ts lines 952-989 (nearestCandidateScore + applyQuaternion helpers — these are display-layer lookup, KEPT per research A4)
    - web/src/profiles/index.ts (the `import type { ScoreWeights } from '../compute'` — must still resolve after strip)
    - web/src/types.ts (the OrientConfig + defaultConfig — SEPARATE file, untouched in this task)
    - .planning/phases/05-rust-consolidation/05-CONTEXT.md lines 67-72 (TS Thin Layer — locked: KEEP types + WEIGHT_PRESETS + loadProfiles; DELETE all metric/ranking/selection/yaw/geometry functions)
    - .planning/phases/05-rust-consolidation/05-RESEARCH.md lines 791-793 (Deprecated: listed TS functions to delete)
  </read_first>
  <files>web/src/compute.ts, web/src/nearestScore.ts</files>
  <action>
Per D-06 (TS Thin Layer locked). The target state for `web/src/compute.ts`: ONLY the following top-level declarations:
- `export interface OriData { positions, normals, areas, directions: Float32Array }` (lines 1-6 — kept verbatim)
- `export interface Candidate { id, quaternion, overhangPenalty, footprint, maxCross, shadowed, surfaceQuality, estHeight, refinedOverhang, refineVariance, stability, stabilityMargin, contactArea, compositeScore }` (lines 8-23 — kept verbatim)
- `export interface ComputeConfig { criticalAngleDeg, excludeUnstable, maxCandidates, refineIterations? }` (lines 25-30 — kept verbatim)
- `export interface SliceResult { penalty, footprint, maxCross, shadowed, surface, quaternion, height, refinedOverhang, refineVariance, stability, stabilityMargin, contactArea, idx, dir }` (lines 32-47 — kept verbatim)
- `export type RefineFn = (...) => number[] | Float32Array` (lines 52-58 — kept verbatim; it remains a type definition even though the worker no longer uses the TS refineFn flow)
- `export interface ScoreWeights { wOverhang, wFootprint, wCross, wSurface, wHeight }` (lines 764-770 — moved up; KEEP)
- `import { loadProfiles } from './profiles'; export const WEIGHT_PRESETS: Record<string, ScoreWeights> = loadProfiles();` (lines 772-773 — moved up; KEEP — per D-06 explicit "Keep in TS: WEIGHT_PRESETS + loadProfiles")

DELETE every other function in compute.ts (per D-06):
- `directionFromIndex` (line 60) — DELETE
- `cross` (line 65) — DELETE (geometry helper)
- `perpendicularBasis` (line 69) — DELETE
- `convexHull2D` (line 79) — DELETE
- `polygonArea` (line 100) — DELETE
- `pointInConvexPolygon` (line 111) — DELETE
- `minEdgeDistance` (line 125) — DELETE
- `angleBetween` (line 144) — DELETE (Rust has it now)
- `scoreCandidate` (line 149) — DELETE (Rust has score_candidate)
- `checkStability` (line 169) — DELETE (Rust has check_stability)
- `computeDefaultYaw` (line 221) — DELETE (Rust has yaw::bbox_min_yaw + full_quaternion)
- `computeHeight` (line 258) — DELETE (Rust has min_z_height)
- `decimateForScore` (line 274) — KEEP (per D-06 explicit "Keep in TS: decimateForScore — or move to Rust (planner decides)"; planner keeps in TS — it's a sampling function, not a metric; the WASM `score_all_directions` receives pre-decimated data; research open question 1 confirms this discretion)
- `footprintArea` (line 312) — DELETE (Rust has footprint_area)
- `misalignmentScore` (line 335) — DELETE (Rust has misalignment_score)
- `maxCrossSection` (line 362) — DELETE (Rust has max_cross_section)
- `shadowedOverhangFraction` (line 403) — DELETE (Rust has shadowed_overhang_fraction)
- `minShadowedOverhang` (line 519) — DELETE (folded into score_all_directions per A7)
- `quaternionAlign` (line 547) — DELETE (Rust has yaw::quaternion_align)
- `multiplyQuats` (line 567) — DELETE (Rust has yaw::multiply_quats)
- `computeSlice` (line 582) — DELETE (replaced by WASM score_all_directions per D-01)
- `mergeCandidates` (line 657) — DELETE (Rust has selection::merge_candidates, called inside WASM rank_candidates or compose in worker)
- `rankByWeights` (line 780) — DELETE (Rust has ranking::rank_by_weights)
- `rankByConsensus` (line 832) — DELETE (Rust has ranking::rank_by_consensus)
- `rankByTopsis` (line 870) — DELETE (Rust has ranking::rank_by_topsis)

EXTRACT to NEW file `web/src/nearestScore.ts` (research A4 — display-layer, kept in TS):
- `export function nearestCandidateScore(quaternion, candidates): { score, index }` (lines 952-973 — MOVE THIS ENTIRE FUNCTION VERBATIM to nearestScore.ts)
- The `applyQuaternion` helper (lines 975-989) — MOVE IT TOO as an internal/private function in nearestScore.ts (used by nearestCandidateScore; not exported)

After the strip, `web/src/compute.ts` should be roughly 80 lines: type definitions + the `decimateForScore` function (sampling utility, kept) + the WEIGHT_PRESETS import. Run `npx tsc --noEmit` and fix any broken imports (other files importing the deleted functions from compute.ts will be broken — those are addressed in Task 4 when main.ts is updated).
  </action>
  <verify>
    <automated>cd web &amp;&amp; wc -l src/compute.ts; grep -v '^#' src/compute.ts | grep -cE '^export (function|const|async function) '; grep -c 'nearestCandidateScore' src/compute.ts; test -f src/nearestScore.ts &amp;&amp; grep -c 'export function nearestCandidateScore' src/nearestScore.ts</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/compute.ts` line count is between 60 and 120 (was 989 — stripped to types + decimateForScore + WEIGHT_PRESETS only).
    - `grep -v '^#' web/src/compute.ts | grep -cE '^export (function|const|async function) '` returns exactly 2: `decimateForScore` and `WEIGHT_PRESETS`.
    - `grep -c 'scoreCandidate\|mergeCandidates\|computeSlice\|rankByWeights\|rankByConsensus\|rankByTopsis\|computeDefaultYaw\|checkStability\|footprintArea\|misalignmentScore\|maxCrossSection\|shadowedOverhangFraction\|minShadowedOverhang\|quaternionAlign\|multiplyQuats\|convexHull2D\|polygonArea\|pointInConvexPolygon\|minEdgeDistance\|angleBetween\|computeHeight\|directionFromIndex\|perpendicularBasis' web/src/compute.ts` returns 0 (no metric/ranking/selection/yaw/geometry functions remain).
    - `web/src/compute.ts` still contains `export interface OriData`, `export interface Candidate`, `export interface ComputeConfig`, `export interface SliceResult`, `export interface ScoreWeights`, `export type RefineFn`, `export function decimateForScore`, `WEIGHT_PRESETS`.
    - `web/src/compute.ts` does NOT contain `nearestCandidateScore` (it moved).
    - `web/src/nearestScore.ts` exists and contains `export function nearestCandidateScore` (verify `grep -c 'export function nearestCandidateScore' web/src/nearestScore.ts` == 1).
    - `web/src/nearestScore.ts` also contains the `applyQuaternion` helper as a non-exported function (verify `grep -c 'function applyQuaternion' web/src/nearestScore.ts` == 1).
  </acceptance_criteria>
  <done>compute.ts is types + decimateForScore + WEIGHT_PRESETS only; nearestCandidateScore + applyQuaternion moved to nearestScore.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Delete web/src/compute.test.ts entirely</name>
  <read_first>
    - web/src/compute.test.ts (full file — 614 lines — know what's being deleted)
    - .planning/phases/05-rust-consolidation/05-CONTEXT.md lines 45-51 (Test Strategy locked: remove compute.test.ts entirely; KEEP quaternion.test.ts, rotate.test.ts, convention.test.ts, centering.test.ts)
  </read_first>
  <files>web/src/compute.test.ts</files>
  <action>
Per D-03 (Test Strategy locked): Remove `web/src/compute.test.ts` entirely. This file contains 614 lines / 52 tests for the TS metric/ranking duplicates being deleted in Task 1. Every function those tests exercised (`scoreCandidate`, `footprintArea`, `maxCrossSection`, `misalignmentScore`, `shadowedOverhangFraction`, `computeHeight`, `checkStability`, `computeDefaultYaw`, `rankByWeights`, `rankByConsensus`, `rankByTopsis`, `mergeCandidates`, `computeSlice`, `minShadowedOverhang`, `quaternionAlign`, `multiplyQuats`) now lives in Rust with ground-truth tests there (Plan 01).

Use `rm web/src/compute.test.ts` (the file is gone, not emptied — D-03 says "Remove `compute.test.ts` entirely").

After deletion, run `npx vitest run` to confirm:
- TS test files still passing: `quaternion.test.ts`, `rotate.test.ts`, `convention.test.ts`, `centering.test.ts` (these are rendering-layer tests, KEPT per D-03).
- No "module not found" errors — if any other file imports from compute.test.ts (it shouldn't — test files aren't imported), fix it.
- The vitest runner finds fewer test files but the kept tests all pass.
  </action>
  <verify>
    <automated>cd web &amp;&amp; test ! -f src/compute.test.ts &amp;&amp; echo "DELETED OK"; npx vitest run 2&gt;&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `test -f web/src/compute.test.ts` fails (file does not exist — verify `test ! -f web/src/compute.test.ts &amp;&amp; echo DELETED`).
    - `git status --porcelain web/src/compute.test.ts` shows ` D web/src/compute.test.ts` (deletion staged for git).
    - `npx vitest run` exits 0 — all 4 remaining TS test files pass (`quaternion.test.ts`, `rotate.test.ts`, `convention.test.ts`, `centering.test.ts`).
    - The vitest summary reports exactly 4 test files (down from 5).
    - No test references `compute.test.ts` or any deleted function (verify `grep -rE 'compute\.test|rankByConsensus|rankByWeights|rankByTopsis|mergeCandidates' web/src --include='*.test.ts'` returns no matches).
  </acceptance_criteria>
  <done>compute.test.ts is deleted; remaining 4 TS rendering-layer test files pass.</done>
</task>

<task type="auto">
  <name>Task 3: Simplify orient.worker.ts to a single-call WASM dispatcher</name>
  <read_first>
    - web/src/orient.worker.ts (full 37 lines — current multi-worker computeSlice dispatcher)
    - web/src/compute.ts (the stripped version from Task 1 — types only + decimateForScore + WEIGHT_PRESETS)
    - ./web/pkg/orient_core.d.ts (from Plan 02 — verify the new exports `score_all_directions`, `rank_candidates`, `compute_norm_bounds` are visible)
    - .planning/phases/05-rust-consolidation/05-RESEARCH.md lines 482-488 (Pitfall 1 — keep ONE thin worker; do NOT call WASM on the main thread)
    - .planning/phases/05-rust-consolidation/05-CONTEXT.md lines 33-37 (Architecture: Single WASM Call locked — workers eliminated or reduced to thin dispatchers; agent discretion allows keeping a thin dispatcher)
  </read_first>
  <files>web/src/orient.worker.ts</files>
  <action>
Per D-01 (Single WASM Call locked) + research Pitfall 1 (keep ONE thin worker to avoid main-thread blocking — this preserves D-01's intent of eliminating worker COORDINATION complexity while keeping UI responsive).

Rewrite `web/src/orient.worker.ts` from scratch. Target body (~50 lines):

Imports at top:
- `import type { OriData, ComputeConfig, Candidate, SliceResult } from './compute';` (OriData/ComputeConfig kept in stripped compute.ts)
- WASM import: `let wasmReady: Promise<any> | null = null;` then `ensureWasm()` helper keeps existing wasm-init (lines 6-17 — keep verbatim — calls `import('../pkg/orient_core.js').then((mod) => { if (mod.init) mod.init(); return mod; })`).

`self.onmessage = async (e: MessageEvent) => { ... }`:
- Destructure `{ data, config }` from `e.data` (the worker no longer takes `dirStart`/`dirCount` — it processes ALL directions in a single WASM call).
- `const wasm = await ensureWasm();` (existing helper).
- Build Float32Array inputs from `data.positions`, `data.normals`, `data.areas`, `data.directions` (they should already be Float32Array after main.ts decimation).
- Define a `progressFn` JS callback that posts `{type: 'progress', value: pct}` — wrap it as a `js_sys::Function`-compatible callable from JS. The wasm export accepts `Function | undefined`; from the worker, pass a plain `(i, total) => self.postMessage({type:'progress', value: Math.round(i/total*100)})`. (Note: the WASM `score_all_directions` accepts an `Option<&js_sys::Function>` — JS passes a regular function reference.)
- Call `const metrics = wasm.score_all_directions(positions, normals, areas, directions, config.criticalAngleDeg, config.refineIterations ?? 0, config.excludeUnstable, progressFn);` — this returns an `N×13` Float32Array ripe for direct consumption.
- Optionally call `const ranked = wasm.rank_candidates(metrics, weights, currentRanker)` IF main.ts passes `weights` and `ranker` in the message payload. Otherwise main.ts does the ranking (simpler — leave it to main.ts to construct the ranker call). Default to: worker ONLY scores; ranking is a separate message or done in main.ts.
- Convert the N×13 Float32Array into a `SliceResult[]` (or `Candidate[]` directly — match the existing `SliceResult`/`Candidate` shape that main.ts expects) by chunking 13 floats per direction and mapping to the TS interface fields. For each direction i: `{ idx: i, dir: [metrics[i*13+0]... wait, quaternion comes first] }`. Decode order per `score_all_directions` Task 1 of Plan 02: `[qx, qy, qz, qw, overhang, footprint, cross, surface, height, shadowed, stable, margin, contact_area]`. Build:
  ```
  {
    penalty: metrics[i*13+4],  // overhang
    footprint: metrics[i*13+5],
    maxCross: metrics[i*13+6],
    surface: metrics[i*13+7],
    height: metrics[i*13+8],
    shadowed: metrics[i*13+9],
    quaternion: [metrics[i*13+0], metrics[i*13+1], metrics[i*13+2], metrics[i*13+3]],
    refinedOverhang: metrics[i*13+4],  // = penalty for now; refine affects via WASM
    refineVariance: 0,  // computed by WASM internally (or 0 if no refine); TODO: extend later
    stability: metrics[i*13+10] > 0.5,
    stabilityMargin: metrics[i*13+11],
    contactArea: metrics[i*13+12],
    idx: i,
    dir: [data.directions[i*3], data.directions[i*3+1], data.directions[i*3+2]],
  }
  ```
  NOTE: `refinedOverhang`/`refineVariance` — if `config.refineIterations > 0`, the WASM `score_all_directions` internally refines and `metrics[i*13+4]` is the refined overhang (per Plan 02 Task 1). If `refine_iters == 0`, it's the raw overhang. `refineVariance` is currently not in the 13-float output (the WASM produces min-refined-overhang but not variance across k runs). For Phase 5, set `refineVariance = 0` in the worker decode and note this as a follow-up — the variance metric was a Phase 3.5 add-on and is not in the locked Phase 5 spec.
- Post `{ type: 'results', candidates: finalCandidates }` back to main.ts (the candidates are the final ranked + selected list with `compositeScore` filled in by `rank_candidates`).

Plan 02 confirmed `web/pkg/orient_core.d.ts` contains `score_all_directions` — verify before wiring.

Do NOT remove `RefineFn` type from compute.ts — it's now unused but kept as a type for backward API stability. (Alternatively delete it too in Task 1 — but Task 1 already keeps it per the locked D-06 list.)
  </action>
  <verify>
    <automated>cd web &amp;&amp; cat src/orient.worker.ts | wc -l; npx tsc --noEmit src/orient.worker.ts 2&gt;&amp;1 | grep -c 'error TS' || echo "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/orient.worker.ts` line count is ≤ 80 (was 37; the new body adds WASM-call logic but stays small).
    - `grep -c 'computeSlice' web/src/orient.worker.ts` == 0 (the old dispatcher is gone).
    - `grep -c 'score_all_directions' web/src/orient.worker.ts` == 1 (single WASM call for all directions).
    - `grep -c 'dirStart\|dirCount' web/src/orient.worker.ts` == 0 (the multi-worker chunking fields are gone).
    - `grep -c 'progress' web/src/orient.worker.ts` ≥ 1 (progress callback wired).
    - `npx tsc --noEmit web/src/orient.worker.ts` reports 0 errors (verify with `npx tsc --noEmit web/src/orient.worker.ts 2&gt;&amp;1 | grep -c 'error TS'` — should be 0; if other files break, those are fixed in Task 4).
    - The worker posts `{ type: 'results', candidates }` (verify `grep -c "type: 'results'" web/src/orient.worker.ts` == 1).
    - The worker calls `rank_candidates` and `select_diverse` (verify `grep -cE 'rank_candidates|select_diverse' web/src/orient.worker.ts` ≥ 2).
  </acceptance_criteria>
  <done>Worker is a thin single-call WASM dispatcher — calls `score_all_directions` → `rank_candidates` → `select_diverse`, posts 'results' with final Candidate[]. Multi-worker chunking is gone.</done>
</task>

<task type="auto">
  <name>Task 4: Update main.ts to use WASM scoring/ranking through the single worker</name>
  <read_first>
    - web/src/main.ts (full 585 lines — locate every reference to deleted TS functions)
    - web/src/main.ts lines 1-30 (imports — to remove the deleted rankByWeights/rankByTopsis/rankByConsensus/mergeCandidates imports)
    - web/src/main.ts lines 220-250, 300-360 (worker dispatch + computeSlice call sites)
    - web/src/main.ts lines 525-545 (recompute path via decimateForScore)
    - web/src/orient.worker.ts (the simplified worker from Task 3 — know its message protocol)
    - web/src/compute.ts (the stripped version — types + decimateForScore + WEIGHT_PRESETS)
    - web/src/nearestScore.ts (extracted helper — main.ts uses nearestCandidateScore for overlay live-score if applicable)
    - web/src/profiles/index.ts (imports `ScoreWeights` from compute — must still resolve)
  </read_first>
  <files>web/src/main.ts, web/src/profiles/index.ts</files>
  <action>
Update `web/src/main.ts` to remove all dependencies on deleted TS functions and use the single-worker + WASM-ranking path. Plan 02 Task 1 has been amended to add a 4th WASM export `select_diverse` (wraps `selection::merge_candidates`) — verify it is visible in `web/pkg/orient_core.d.ts` before wiring.

Imports (top of file):
- REMOVE: `import { mergeCandidates, rankByConsensus, rankByWeights, rankByTopsis, WEIGHT_PRESETS } from './compute';` (line 8)
- REPLACE WITH: `import { WEIGHT_PRESETS } from './compute';` (kept — per D-06 WEIGHT_PRESETS stays in TS)
- If `nearestCandidateScore` is used by main.ts (overlay live-score path), ADD: `import { nearestCandidateScore } from './nearestScore';` (extracted in Task 1)
- KEEP: `import { decimateForScore } from './compute';` (line 7)
- KEEP: `import type { OriData, Candidate, ComputeConfig, SliceResult } from './compute';` (line 9)
- KEEP: `import { score_orientation } from '../pkg/orient_core.js';` (existing — used for overlay live-score, remains)

The multi-worker dispatch (current code at lines 220-360 approx — `workers: Worker[]` array, `hardwareConcurrency - 1` spawning, postMessage per-worker with `dirStart`/`dirCount`, collecting 'slice-done' messages): REPLACE with a single-worker model:
- Spawn ONE worker: `const worker = new Worker(new URL('./orient.worker.ts', import.meta.url), { type: 'module' });`
- Post ONE message `{ data: decimated, config: computeConfig }` (no dirStart/dirCount — Task 3 worker ignores them).
- Listen for ONE `'results'` message (not multiple 'slice-done'); on receipt, the candidates are the final ranked + selected list with `compositeScore` filled in by the worker's `rank_candidates` + `select_diverse` calls.
- Apply `selection::merge_candidates`-equivalent diversity selection: this happens inside WASM `rank_candidates` IF the function does selection. (Per Plan 02 Task 1, `rank_candidates` only ranks — it does NOT do angle-diversity selection. So main.ts must either (a) call WASM `rank_candidates` AND then do TS-side selection, OR (b) extend the worker to do selection post-ranking.)

Resolution (planner discretion): keep `merge_candidates` selection in the worker (the worker runs `score_all_directions`, then optionally `rank_candidates`, then applies TS-side angle-diversity selection per the ranked list). But selection was JUST deleted from TS! So either:
- Add a TS angle-diversity helper IN the worker (5-10 lines — port `mergeCandidates`'s selection loop only) — keeps the ranking module's `merge_candidates` Rust version authoritative, but the worker needs its own thin TS diver for the diversity filtering. **NO** — this reintroduces a TS duplicate. Bad.
- Extend the WASM `rank_candidates` API to include selection (return ranked + selected indices). **YES** — this matches the "single source of truth" goal.

**Decision: Use the 3 WASM exports sequentially** — `score_all_directions` → `rank_candidates` → `select_diverse` — all called inside the worker as a 3-step pipeline. Plan 02 has been amended (Task 1) to include the 4th export `select_diverse` so this is fully supported.

For Plan 03 (this task): the worker calls `wasm.score_all_directions(...)` (Plan 02 export) then `wasm.rank_candidates(metrics, weights, ranker)` (Plan 02 export) then `wasm.select_diverse(ranked, directions, stable_flags, exclude_unstable, max_candidates, 15.0)` (Plan 02 export), and posts the final `Candidate[]` (with `compositeScore` filled in) back to main.ts. ALL of these happen sequentially inside the worker — D-01's "single WASM call" intent is preserved (one worker message → one logical scoring pipeline call; the 3 internal steps are exposed for cleanliness, but main.ts sees ONE worker round-trip).

Update main.ts to:
- Spawn ONE worker, send `{ data, config, weights, ranker, maxCandidates, minAngleDeg }` in the postMessage.
- Listen for ONE message of `{ type: 'results', candidates: Candidate[] }` — the candidates are the final ranked + selected list.
- Re-spawn / re-message on profile/ranker/angle change ("Recalculate" button at bottom of config panel, existing — line 308 of main.ts uses `WEIGHT_PRESETS` + `applyCurrentRank`).
- The `applyCurrentRank` helper (line 308 approx) must be CHANGED: it currently calls TS `rankByWeights`/`rankByConsensus`/`rankByTopsis`/`mergeCandidates`. Replace the entire body with a worker postMessage (the cached `allSlices` raw metrics may need to be re-sent; OR re-call `score_all_directions` — simpler). Decision: on `applyCurrentRank`, call the worker again with the SAME data + new ranker/weights → worker re-ranks via `rank_candidates` + `select_diverse` and posts back. Cheap because workers cache the WASM module.

For the `computeNormBounds` path in main.ts (line 33 approx — `score_orientation` based): replace with a direct call to `wasm.compute_norm_bounds(data.positions, data.normals, data.areas, data.directions, config.criticalAngleDeg)`. The return is 10 floats `[lo[5], hi[5]]`. This is a synchronous WASM call from main.ts (light — ~30 directions × cheap metrics), no worker needed for this step.

Update `web/src/profiles/index.ts` to import `ScoreWeights` from `'../compute'` (it already does — verify the import still resolves after Task 1 strip).

Rebuild WASM to ensure the `select_diverse` export is picked up if Plan 02 was amended: `npm run build:wasm`. Then run `npm run type-check`, `npm run build`, `npm test` in the web/ directory.
  </action>
  <verify>
    <automated>cd /home/jorge/Sandbox/orient-stl &amp;&amp; wasm-pack build core --target bundler --out-dir web/pkg 2&gt;&amp;1 | tail -3; cd web &amp;&amp; npx tsc --noEmit 2&gt;&amp;1 | grep -c 'error TS'; npm run build 2&gt;&amp;1 | tail -3; npx vitest run 2&gt;&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/main.ts` no longer imports `mergeCandidates`, `rankByConsensus`, `rankByWeights`, `rankByTopsis` (verify `grep -c 'mergeCandidates\|rankByConsensus\|rankByWeights\|rankByTopsis' web/src/main.ts` == 0).
    - `web/src/main.ts` still imports `decimateForScore` and `WEIGHT_PRESETS` (verify `grep -c 'decimateForScore\|WEIGHT_PRESETS' web/src/main.ts` ≥ 1 each).
    - `web/src/main.ts` calls `wasm.score_all_directions` (via worker) and `wasm.rank_candidates` and `wasm.compute_norm_bounds` (directly or via worker) — verify each appears in the file: `grep -cE 'score_all_directions|rank_candidates|compute_norm_bounds' web/src/main.ts` ≥ 3 (counting worker message payloads + direct calls).
    - Single-worker model: `grep -c 'new Worker' web/src/main.ts` == 1 (only one Worker constructor — no more `workers[k]` array).
    - `grep -c 'hardwareConcurrency' web/src/main.ts` == 0 (no multi-worker dispatch based on cores).
    - `npx tsc --noEmit` exits 0 (no TS errors — verify `npx tsc --noEmit 2&gt;&amp;1 | grep -c 'error TS'` is 0).
    - `npm run build` (vite build) exits 0.
    - `npx vitest run` exits 0 — the 4 remaining TS test files (quaternion, rotate, convention, centering) still pass.
    - `wasm-pack build core --target bundler --out-dir web/pkg` exits 0.
  </acceptance_criteria>
  <done>main.ts uses single-worker + WASM ranking/selection/norm_bounds; all browser builds and TS tests pass; no multi-worker dispatch remains.</done>
</task>

<task type="auto">
  <name>Task 5: Final audit — verify single source of truth in Rust, no TS duplicates</name>
  <read_first>
    - web/src/compute.ts (the stripped final version)
    - web/src/main.ts (the updated final version)
    - web/src/orient.worker.ts (the simplified final version)
    - .planning/phases/05-rust-consolidation/05-CONTEXT.md lines 47-72 (TS Thin Layer locked: what to keep vs delete)
    - .planning/phases/05-rust-consolidation/05-RESEARCH.md lines 791-793 (Deprecated function list — all should be absent from TS)
    - ./web/pkg/orient_core.d.ts (verify the 4 WASM exports are visible)
  </read_first>
  <files>(no source files modified — audit only)</files>
  <action>
Run a comprehensive audit confirming the success criteria of Phase 5 are met by the TS layer (Plan 03 contribution):

1. **No TS metric/ranking/selection/yaw/geometry functions exist** — run a grep across `web/src/` for each deleted function name. Each must return 0 results:
   - `scoreCandidate`, `footprintArea`, `maxCrossSection`, `misalignmentScore`, `shadowedOverhangFraction`, `computeHeight`, `checkStability`, `computeDefaultYaw`, `rankByWeights`, `rankByConsensus`, `rankByTopsis`, `mergeCandidates`, `computeSlice`, `minShadowedOverhang`, `quaternionAlign`, `multiplyQuats`, `convexHull2D`, `polygonArea`, `pointInConvexPolygon`, `minEdgeDistance`, `angleBetween`, `directionFromIndex`, `perpendicularBasis`
   - Allowed exceptions: `decimateForScore` (sampling — kept per D-06), `nearestCandidateScore` (display-layer, moved to nearestScore.ts — kept per research A4), `applyQuaternion` (display-layer helper inside nearestScore.ts — kept).

2. **compute.test.ts is deleted** — `test ! -f web/src/compute.test.ts`.

3. **4 TS test files remain and pass** — `quaternion.test.ts`, `rotate.test.ts`, `convention.test.ts`, `centering.test.ts` — `npx vitest run` reports 4 test files, all passing.

4. **WASM exports visible** — `grep -cE 'score_all_directions|rank_candidates|compute_norm_bounds|select_diverse' web/pkg/orient_core.d.ts` returns exactly 4 (all 4 exports visible from JS per the amended Plan 02 Task 1).

5. **TS type-check + build pass** — `npx tsc --noEmit` and `npm run build` both exit 0.

6. **No imports of deleted functions** — `grep -rE "from './compute'" web/src/ --include='*.ts' --include='*.tsx'` should show imports only of `OriData`, `Candidate`, `ComputeConfig`, `SliceResult`, `ScoreWeights`, `RefineFn`, `decimateForScore`, `WEIGHT_PRESETS` (types + decimateForScore + WEIGHT_PRESETS).

Capture the full audit output (grep counts + tsc/vitest/build exits) into `.planning/phases/05-rust-consolidation/05-03-AUDIT.md` so Plan 04 verification can reference it.
  </action>
  <verify>
    <automated>cd /home/jorge/Sandbox/orient-stl/web &amp;&amp; for fn in scoreCandidate footprintArea maxCrossSection misalignmentScore shadowedOverhangFraction computeHeight checkStability computeDefaultYaw rankByWeights rankByConsensus rankByTopsis mergeCandidates computeSlice minShadowedOverhang quaternionAlign multiplyQuats convexHull2D polygonArea pointInConvexPolygon minEdgeDistance angleBetween directionFromIndex perpendicularBasis; do count=$(grep -rE "\b$fn\b" src/ --include='*.ts' --include='*.tsx' | grep -v 'compute.test.ts' | grep -v 'nearestScore.ts' | grep -v 'applyQuaternion' | wc -l); echo "$fn: $count"; done; npx tsc --noEmit 2&gt;&amp;1 | tail -3; npx vitest run 2&gt;&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Every function in the audit list returns count 0 (no TS duplicate exists).
    - `test ! -f web/src/compute.test.ts` passes (file deleted).
    - `npx tsc --noEmit` exits 0.
    - `npm run build` exits 0.
    - `npx vitest run` exits 0 with exactly 4 test files passing.
    - `grep -cE 'score_all_directions|rank_candidates|compute_norm_bounds' web/pkg/orient_core.d.ts` returns ≥ 3 (all new WASM exports visible from JS).
    - `.planning/phases/05-rust-consolidation/05-03-AUDIT.md` exists and contains the audit grep counts.
  </acceptance_criteria>
  <done>TS layer audit confirms zero duplicates; only rendering-layer tests remain; WASM exports visible; builds + tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JS main thread ↔ worker | Single worker postMessage — minimal data crossing, typed `OriData` payload only |
| Worker ↔ WASM | `&[f32]` slices crossing FFI — no path/string handling, no state mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | DoS | Large mesh blocks UI thread if WASM called from main.ts | mitigate | Worker is kept as thin WASM dispatcher (research Pitfall 1) — `score_all_directions` runs off the main thread; progress callback via postMessage |
| T-05-06 | Tampering | Malformed float array from worker message | mitigate | Worker decodes N×13 with explicit chunking; bounds check by direction count == directions.length/3 |
| T-05-SC | Tampering | No new package installs in this plan | accept | TS deletions only — no new npm deps. Pitfall 5 mitigated via `wasm-pack build` in Task 4. |
</threat_model>

<verification>
After all 5 tasks complete:
- `web/src/compute.ts` is ≤ 120 lines, contains only types + `decimateForScore` + `WEIGHT_PRESETS`
- `web/src/compute.test.ts` does not exist
- `web/src/nearestScore.ts` contains the extracted `nearestCandidateScore` display helper
- `web/src/orient.worker.ts` posts ONE 'results' message after calling `score_all_directions` + `rank_candidates` + `select_diverse`
- `web/src/main.ts` spawns ONE worker, no `hardwareConcurrency` references, no deleted-function imports
- All WASM exports visible in `web/pkg/orient_core.d.ts`
- `npx tsc --noEmit`, `npm run build`, `npx vitest run` all exit 0
- `.planning/phases/05-rust-consolidation/05-03-AUDIT.md` contains the audit record
</verification>

<success_criteria>
- TS is rendering-only — no metric, ranking, selection, yaw, or geometry function in TS (except `decimateForScore` per D-06 planner discretion + `nearestCandidateScore` + `applyQuaternion` per research A4)
- compute.test.ts deleted (D-03)
- 4 TS rendering-layer test files retained and pass (D-03): quaternion, rotate, convention, centering
- Single worker action: postMessage → WASM score_all_directions + WASM rank_candidates + WASM select_diverse → 'results' (D-01)
- main.ts has no multi-worker dispatch, no `hardwareConcurrency` spawning
- WASM rebuilt; all exports visible from TS
- `npm run type-check`, `npm run build`, `npm test` all pass
</success_criteria>

<output>
Create `.planning/phases/05-rust-consolidation/05-03-SUMMARY.md` and `.planning/phases/05-rust-consolidation/05-03-AUDIT.md` when done
</output>

## Artifacts this phase produces

### Modified TS files
- `web/src/compute.ts` — stripped to type definitions + decimateForScore + WEIGHT_PRESETS (≈80 lines, was 989)
- `web/src/orient.worker.ts` — thin single-call WASM dispatcher (score_all_directions → rank_candidates → select_diverse → postMessage 'results')
- `web/src/main.ts` — single-worker model, no hardwareConcurrency, calls WASM via worker for ranking/selection, calls WASM `compute_norm_bounds` directly for live-scoring bounds, no deleted function imports
- `web/src/profiles/index.ts` — verified import of `ScoreWeights` from stripped compute.ts still resolves

### New TS files
- `web/src/nearestScore.ts` — `nearestCandidateScore` + `applyQuaternion` (display-layer, kept per research A4)

### Deleted
- `web/src/compute.test.ts` (614 lines, 52 tests — per D-03)

### Audit artifact
- `.planning/phases/05-rust-consolidation/05-03-AUDIT.md` — grep counts proving no TS duplicates remain

### Cross-plan dependency (resolved during planning)
- Plan 02 Task 1 was amended to include the 4th WASM export `select_diverse(ranked, directions, stable_flags, exclude_unstable, max_candidates, min_angle_deg) -> Vec<f32>` wrapping `selection::merge_candidates`. Plan 03's worker uses it as the 3rd step of the single-worker pipeline (`score_all_directions` → `rank_candidates` → `select_diverse`). Both plans ship the export and the consumer in the same wave pair (Wave 2 → Wave 3).