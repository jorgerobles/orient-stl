---
phase: 05-rust-consolidation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - core/Cargo.toml
  - core/src/ranking.rs
  - core/src/selection.rs
  - core/src/yaw.rs
  - core/src/lib.rs
  - core/src/candidates.rs
autonomous: true
requirements: []
tags: [rust, ranking, selection, yaw, tdd, ground-truth]

must_haves:
  truths:
    - "rank_by_weights / rank_by_consensus / rank_by_topsis live in Rust and produce identical results to the deleted TS rankByWeights / rankByConsensus / rankByTopsis for the same input metrics"
    - "merge_candidates (angle-diversity selection) lives in Rust and picks the same diverse subset as the deleted TS mergeCandidates"
    - "yaw::full_quaternion(dir, mesh) produces the SAME orientation as the deleted TS qFull = multiplyQuats(computeDefaultYaw(dir, verts), quaternionAlign(dir, [0,-1,0]))"
    - "Every new Rust test is ground-truth: expected values come from hand-computed geometry, not from running the implementation"
    - "core compiles under both default features (wasm) and --no-default-features (pure core)"
  artifacts:
    - path: "core/src/ranking.rs"
      provides: "rank_by_weights, rank_by_consensus, rank_by_topsis + ScoreWeights / CandidateMetrics structs"
      contains: "pub(crate) fn rank_by_weights"
    - path: "core/src/selection.rs"
      provides: "merge_candidates (angle-diversity), angle_between"
      contains: "pub(crate) fn merge_candidates"
    - path: "core/src/yaw.rs"
      provides: "quaternion_align, multiply_quats, bbox_min_yaw, full_quaternion"
      contains: "pub(crate) fn full_quaternion"
    - path: "core/Cargo.toml"
      provides: "Dual-target feature scaffolding (wasm + cli features, optional deps)"
      contains: 'features'
  key_links:
    - from: "core/src/ranking.rs"
      to: "core/src/scoring.rs (CandidateMetrics / ScoreComponents)"
      via: "struct reuse"
      pattern: "use crate::scoring::"
    - from: "core/src/yaw.rs::full_quaternion"
      to: "TS qFull convention (multiplyQuats(qYaw, qAlign(dir, -Y)))"
      via: "faithful port"
      pattern: "multiply_quats.*quaternion_align"
---

<objective>
Port the three TS ranking algorithms (weighted-sum, consensus, TOPSIS), the angle-diversity candidate selection (`mergeCandidates`), and the bbox-minimizing yaw + quaternion alignment helpers from `web/src/compute.ts` into new Rust modules in `core/src/`. Every port is a faithful translation — same formulas, same normalization, same sort order — verified by ground-truth (hand-computed) tests only. Per D-04 (locked ground-truth strategy) and D-08 (reuse/keep existing Rust code).

Purpose: Establish the single Rust source of truth for ranking + selection + yaw before the WASM `score_all_directions` export (Plan 02) and TS deletion (Plan 03) build on top of it.
Output: Three new Rust modules (`ranking.rs`, `selection.rs`, `yaw.rs`) with ground-truth tests, plus Cargo.toml dual-target scaffolding that enables the CLI binary in Plan 02.
</objective>

<execution_context>
@/home/jorge/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/jorge/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-rust-consolidation/05-CONTEXT.md
@.planning/phases/05-rust-consolidation/05-RESEARCH.md
@.opencode/skills/spike-findings-orient-stl/SKILL.md

# Source files the executor MUST read before implementation
@web/src/compute.ts
@core/src/scoring.rs
@core/src/stability.rs
@core/src/candidates.rs
@core/src/lib.rs
@core/Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cargo.toml dual-target feature scaffolding</name>
  <read_first>
    - core/Cargo.toml (current full contents)
    - .planning/phases/05-rust-consolidation/05-RESEARCH.md (lines 137-162 — Cargo.toml dual-target pattern with exact TOML)
  </read_first>
  <files>core/Cargo.toml</files>
  <action>
Per D-02 (CLI in same crate — locked) and research Pattern 1, refactor core/Cargo.toml for dual-target builds (WASM cdylib + native CLI binary).

Keep unchanged: `[lib]` with `crate-type = ["cdylib", "rlib"]`, `[dependencies]` with `serde = { version = "1", features = ["derive"] }` and `stl_io = "0.11"`, `[profile.release]` with `opt-level = "s"`.

Make these dependencies OPTIONAL (feature-gated):
- `wasm-bindgen = { version = "0.2", optional = true }`
- `serde-wasm-bindgen = { version = "0.6", optional = true }`
- `js-sys = { version = "0.3.103", optional = true }`
- `console_error_panic_hook = { version = "0.1", optional = true }`

Add these CLI-only dependencies (optional):
- `serde_json = { version = "1", optional = true }`
- `clap = { version = "4", features = ["derive"], optional = true }`

Add the `[features]` section:
```
default = ["wasm"]
wasm = ["wasm-bindgen", "serde-wasm-bindgen", "js-sys", "console-error-panic-hook"]
cli = ["serde_json", "clap"]
```

Do NOT add the `[[bin]]` section in this task — that arrives in Plan 02 Task 4 when the binary file exists. Adding it now would break `cargo build` (the source file does not exist yet).
  </action>
  <verify>
    <automated>cd core &amp;&amp; cargo build &amp;&amp; cargo build --no-default-features &amp;&amp; cargo test --lib</automated>
  </verify>
  <acceptance_criteria>
    - `core/Cargo.toml` contains the literal substring `optional = true` on the lines for wasm-bindgen, serde-wasm-bindgen, js-sys, console_error_panic_hook, serde_json, and clap (6 occurrences).
    - `core/Cargo.toml` contains the literal substring `default = ["wasm"]`.
    - `core/Cargo.toml` contains the literal substring `cli = ["serde_json", "clap"]`.
    - `cargo build` (default features = wasm) exits 0.
    - `cargo build --no-default-features` exits 0 (pure core compiles without wasm-bindgen).
    - `cargo test --lib` exits 0 — existing Rust tests still pass with the new feature setup.
    - `core/Cargo.toml` does NOT contain a `[[bin]]` section yet.
  </acceptance_criteria>
  <done>Cargo.toml is dual-target-ready; all existing Rust tests still pass under default and no-default features.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (RED): Ground-truth tests for ranking + selection + yaw</name>
  <read_first>
    - web/src/compute.ts lines 657-759 (mergeCandidates), 780-823 (rankByWeights), 832-858 (rankByConsensus), 870-948 (rankByTopsis), 547-577 (quaternionAlign + multiplyQuats), 221-256 (computeDefaultYaw)
    - core/src/scoring.rs (ScoreComponents struct at line 193 and existing test pattern)
    - core/src/stability.rs (StabilityResult struct)
    - .planning/phases/05-rust-consolidation/05-CONTEXT.md lines 53-58 (Ground-Truth Test Requirements — locked) and 136-144 (hand-computed test examples)
    - .planning/phases/05-rust-consolidation/05-RESEARCH.md lines 423-461 (Pattern 4: ground-truth test example with hand-computed TOPSIS values)
  </read_first>
  <files>core/src/ranking.rs, core/src/selection.rs, core/src/yaw.rs, core/src/lib.rs</files>
  <behavior>
Per D-04 (ground-truth locked). Every test uses hand-computed expected values from known geometry, NOT consistency comparisons. Write tests FIRST that must fail.

Ranking tests (in `core/src/ranking.rs` `#[cfg(test)] mod tests`):
- Test `rank_by_weights_three_candidates_hand_computed`: 3 candidates with metrics {C0: overhang=0.0, footprint=2.0, cross=1.0, surface=1.0, height=1.0; C1: overhang=1.0, footprint=1.0, cross=1.0, surface=2.0, height=0.5; C2: overhang=0.5, footprint=0.5, cross=0.5, surface=1.5, height=0.0}. Weights {w_overhang:1.0, w_footprint:1.0, w_cross:0.0, w_surface:1.0, w_height:1.0}. Hand-compute min-max normalization per column, surface inverted to cost form `(sH - surf) / sSpan` (per D-07 locked formula), weighted sum, sort ascending. Expected: rank order must be C2 &lt; C0 &lt; C1 (lowest composite = best) — assert verify exact compositeScore values for C0..C2 within 1e-5.
- Test `rank_by_consensus_three_candidates_hand_computed`: SAME 3 candidates. Consensus = 1 − max(normalized costs) where costs include shadowed as a 6th term. For this test, set shadowed = {0.0, 0.5, 1.0} for C0/C1/C2 respectively. Hand-compute the max for each and verify rank order (highest consensus first). Expected C2 &gt; C0 &gt; C1.
- Test `rank_by_topsis_three_candidates_hand_computed`: Use the exact hand-computed TOPSIS values from RESEARCH.md lines 423-461 (3 candidates {C0: ov=0, h=1.0; C1: ov=1, h=0.5; C2: ov=0.5, h=0}, weights {w_overhang:1, w_height:1, others=0}). Expected closeness values: C0=0.0, C1≈0.309, C2≈0.739. Assert within 0.01.
- Test `surface_quality_inverted_in_weighted_sum`: 2 candidates {C0: surface=1.0, C1: surface=2.0} all other metrics equal. Weights {w_surface:1.0, others:0}. C1 (higher surface) must rank HIGHER (lower composite cost) — confirms D-07 inversion. Expected C1 before C0.
- Test `ranking_empty_returns_empty`: All three rank functions on `&[]` return `vec![]`.

Selection tests (in `core/src/selection.rs` `#[cfg(test)] mod tests`):
- Test `merge_candidates_diversity_filter`: 5 directions at angles 0°, 10°, 20°, 30°, 40° from [0,0,1] in the XZ plane, scoring ascending by index (so sorted order is C0..C4). max_candidates=10, min_angle_deg=15°, exclude_unstable=false. Expected: keep 0°, 20°, 40° (each is &gt;15° from prior keep) — assert resul==idx [0, 2, 4].
- Test `merge_candidates_excludes_unstable_when_flagged`: same 5 directions, mark idx 2 as unstable, exclude_unstable=true. Expected result excludes 2.
- Test `merge_candidates_caps_at_max`: 10 directions well-separated, max_candidates=3. Expected result.len() == 3.
- Test `angle_between_orthogonal_is_90`: angle_between([1,0,0],[0,1,0]) == 90.0 ± 1e-4.
- Test `angle_between_parallel_is_0`: angle_between([1,0,0],[1,0,0]) == 0.0.

Yaw tests (in `core/src/yaw.rs` `#[cfg(test)] mod tests`):
- Test `quaternion_align_identity_when_aligned`: a=[0,0,1], b=[0,0,1] → expect [1,0,0,0].
- Test `quaternion_align_z_to_neg_y`: a=[0,0,-1], b=[0,-1,0] → expect quaternion rotates [0,0,-1] to [0,-1,0]. Verify by applying the result q to a (via test-local apply) and asserting the rotated vector ≈ b within 1e-5.
- Test `quaternion_align_negation_180_axis`: a=[0,0,1], b=[0,0,-1] → expect a 180° rotation about a perpendicular axis (w=0), unit quaternion.
- Test `multiply_quats_identity_left`: multiply_quats([1,0,0,0], [w,x,y,z]) == [w,x,y,z].
- Test `multiply_quats_inverse_yields_identity`: q × conjugate(q) ≈ [1,0,0,0] within 1e-5.
- Test `bbox_min_yaw_unit_square_best_angle`: 2×2 unit square (verts (-1,-1,0),(1,-1,0),(1,1,0),(-1,1,0)), dir=[0,0,-1]. Expected best_angle ≈ 0 (or π/2 — both give the same bbox area); assert the returned qYaw is approximately identity (since rotating a square by 90° gives the same bbox).
- Test `full_quaternion_unit_cube_dir_z_neg`: unit cube verts (0,0,0)-(1,1,1), dir=[0,0,-1]. Hand-expect: qAlign aligns [0,0,-1] to [0,-1,0] = 90° rotation about X-axis. qYaw identity (z-aligned cube → square bbox in XY, no yaw optimization changes it). So qFull ≈ 90° rotation about +X: q = [cos(45°), sin(45°), 0, 0] = [0.7071, 0.7071, 0, 0]. Assert within 1e-3. Per D-07 / pitfall #6.
  </behavior>
  <action>
TDD RED phase. Create three new files: `core/src/ranking.rs`, `core/src/selection.rs`, `core/src/yaw.rs`. Each starts with ONLY the struct definitions needed by tests + stub functions that `unimplemented!()` so tests compile and fail at runtime. Declare the modules in `core/src/lib.rs` after the existing `mod rng;` line: add `mod ranking; mod selection; mod yaw;` (use `pub(crate)` visibility in the modules themselves).

In `core/src/ranking.rs` declare:
- pub(crate) struct `ScoreWeights { w_overhang: f32, w_footprint: f32, w_cross: f32, w_surface: f32, w_height: f32 }`
- pub(crate) struct `CandidateMetrics { overhang: f32, footprint: f32, max_cross: f32, surface: f32, height: f32, shadowed: f32 }` (surface = misalignment_score benefit metric per D-07; shadowed included to support TS consensus's 6-term max)
- pub(crate) fn `rank_by_weights(metrics: &[CandidateMetrics], w: &ScoreWeights) -> Vec&lt;(usize, f32)&gt;` stub `unimplemented!()`
- pub(crate) fn `rank_by_consensus(metrics: &[CandidateMetrics]) -> Vec&lt;(usize, f32)&gt;` stub
- pub(crate) fn `rank_by_topsis(metrics: &[CandidateMetrics], w: &ScoreWeights) -> Vec&lt;(usize, f32)&gt;` stub
- A `#[cfg(test)] mod tests` block with all the ground-truth tests listed in &lt;behavior&gt;.

In `core/src/selection.rs` declare:
- pub(crate) fn `merge_candidates(scored: &[(usize, f32)], directions: &[[f32;3]], stable_flags: &[bool], exclude_unstable: bool, max_candidates: usize, min_angle_deg: f32) -> Vec&lt;usize&gt;` stub
- pub(crate) fn `angle_between(a: &[f32;3], b: &[f32;3]) -> f32` stub
- A `#[cfg(test)] mod tests` block with the selection ground-truth tests.

In `core/src/yaw.rs` declare:
- pub(crate) fn `quaternion_align(a: &[f32;3], b: &[f32;3]) -> [f32;4]` stub
- pub(crate) fn `multiply_quats(a: &[f32;4], b: &[f32;4]) -> [f32;4]` stub
- pub(crate) fn `bbox_min_yaw(dir: &[f32;3], mesh: &crate::mesh::MeshData) -> [f32;4]` stub (yaw-only quaternion; faithful port of TS computeDefaultYaw lines 221-256)
- pub(crate) fn `full_quaternion(dir: &[f32;3], mesh: &crate::mesh::MeshData) -> [f32;4]` stub — must return `multiply_quats(&bbox_min_yaw(dir, mesh), &quaternion_align(dir, &[0.0,-1.0,0.0]))`
- A `#[cfg(test)] mod tests` block with all yaw ground-truth tests. Add a small helper `fn apply_quat(q: &[f32;4], v: [f32;3]) -> [f32;3]` inside the test module (Hamilton-product vector rotation) purely for verification asserts. Do NOT export it from the module.

Run `cargo test --lib ranking selection yaw` — every new test MUST fail with "not implemented" panic. RED confirmed.
  </action>
  <verify>
    <automated>cd core &amp;&amp; cargo test --lib ranking 2&gt;&amp;1 | grep -E "test result|panicked" | grep -v "^#"; cargo test --lib selection 2&gt;&amp;1 | grep -E "test result|panicked" | grep -v "^#"; cargo test --lib yaw 2&gt;&amp;1 | grep -E "test result|panicked" | grep -v "^#"</automated>
  </verify>
  <acceptance_criteria>
    - `core/src/ranking.rs` exists and contains `pub(crate) fn rank_by_weights`, `pub(crate) fn rank_by_consensus`, `pub(crate) fn rank_by_topsis`, `pub(crate) struct CandidateMetrics`, `pub(crate) struct ScoreWeights`.
    - `core/src/selection.rs` exists and contains `pub(crate) fn merge_candidates`, `pub(crate) fn angle_between`.
    - `core/src/yaw.rs` exists and contains `pub(crate) fn quaternion_align`, `pub(crate) fn multiply_quats`, `pub(crate) fn bbox_min_yaw`, `pub(crate) fn full_quaternion`.
    - `core/src/lib.rs` declares `mod ranking;`, `mod selection;`, `mod yaw;` after the existing `mod rng;` line.
    - `cargo test --lib ranking` exits with test failures (test result: FAILED) on at least 5 ranking tests.
    - `cargo test --lib selection` exits with test failures on at least 5 selection tests.
    - `cargo test --lib yaw` exits with test failures on at least 7 yaw tests.
    - No test passes (RED state confirmed) — all new tests panic with `not implemented`.
    - `grep -v '^#' core/src/ranking.rs | grep -c 'unimplemented!()'` ≥ 3 (every ranking fn body).
    - `grep -v '^#' core/src/selection.rs | grep -c 'unimplemented!()'` ≥ 2.
    - `grep -v '^#' core/src/yaw.rs | grep -c 'unimplemented!()'` ≥ 4.
  </acceptance_criteria>
  <done>Three new Rust modules declared in lib.rs with stubs; 17+ ground-truth tests exist and ALL fail with "not implemented" (RED phase confirmed).</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3 (GREEN): Implement ranking + selection faithfully from compute.ts</name>
  <read_first>
    - web/src/compute.ts lines 657-759 (mergeCandidates), 780-823 (rankByWeights), 832-858 (rankByConsensus), 870-948 (rankByTopsis)
    - core/src/ranking.rs and core/src/selection.rs (the stubs + tests you wrote in Task 2)
    - .planning/phases/05-rust-consolidation/05-RESEARCH.md lines 609-720 (Example 2 ranking.rs TOPSIS port + Example 3 selection.rs port — reference implementations)
    - .planning/phases/05-rust-consolidation/05-CONTEXT.md lines 75-77 (Surface Quality Formula — locked: surface is BENEFIT, cost form `(sH - surf) / sSpan`)
  </read_first>
  <files>core/src/ranking.rs, core/src/selection.rs</files>
  <behavior>
Implement faithfully — same formulas, same normalization, same sort orders as TS. Per D-07 (locked surface formula reminder), surface is a BENEFIT metric.

`rank_by_weights` (port of `rankByWeights` lines 780-823): min-max normalize each of 5 cost columns (overhang=refinedOverhang, footprint, cross=surface_quality is BENEFIT — invert to cost via `(sMax - surf) / sSpan`, height). Apply weights. Sort ASCENDING by composite (lower = better). Return `Vec&lt;(usize, f32)&gt;` of (original_index, composite_score) sorted. Empty input returns vec![].

`rank_by_consensus` (port of `rankByConsensus` lines 832-858): Per-metric min-max cost normalization. CRITICAL: TS uses SIX terms in the max(): overhang, footprint, cross, **shadowed** (NOT surface as cost separately — shadowed is its own cost term at line 849), surface INVERTED (line 850), height. compositeScore = 1 - max(all 6 terms). Sort DESCENDING (higher consensus = better). Return Vec.

`rank_by_topsis` (port of `rankByTopsis` lines 870-948): Vector normalization (sqrt-sum-of-squares per column). Apply weights. Ideal-best: MIN for cost metrics, MAX for surface (benefit). Compute Euclidean S+ (dist to best) and S- (dist to worst). Surface contribution to S+: `(sBest - ws[i])²`, to S-: `(ws[i] - sWorst)²` — INVERTED compared to cost metrics. Closeness C_i = S-/(S+ + S-) or 1.0 if both 0. Sort DESCENDING. Skip columns where weight = 0. Return Vec.

`merge_candidates` (port of `mergeCandidates` lines 657-759 selection loop only — sort happens BEFORE calling merge): Iterate `scored` (already sorted: weighted-sum by ascending composite, consensus/topsis by descending composite). For each (idx, _score): skip if `exclude_unstable &amp;&amp; !stable_flags[idx]`. Check `angle_between(directions[idx], picked[k]) &gt;= min_angle_deg` for all kept `picked`. If all diverse, push idx to result and direction to picked. Stop at `max_candidates`. Per TS line 732 `minAngle = 15` is hardcoded in selection — keep it parameterized (caller passes 15.0).

`angle_between`: hand-formula `acos(clamp(dot/(|a||b|), -1, 1)) * 180/π`. Handle zero-length vectors (return 0).
  </behavior>
  <action>
Replace stub `unimplemented!()` bodies with faithful Rust translations of the TS logic. Per research "Don't Hand-Roll" (line 476): port the verified-correct TS code directly; do not re-derive.

For `rank_by_weights`: compute min/max for each of 5 columns in one pass; spans use `max(hi - lo, 1e-9)`; surface cost normalization is `(sMax - surf) / sSpan` per D-07 (NOT `(surf - sMin) / sSpan` — pitfall #2 trap). Composite = sum of `w_i * normalized_i`. Use `partial_cmp(...).unwrap_or(Equal)` for f32 sort. Return sorted `Vec&lt;(usize, f32)&gt;`.

For `rank_by_consensus`: 6 terms in the max, NOT 5 — TS line 849 uses `c.shadowed` as a separate cost term alongside `surface` (inverted). Normalize shadowed as cost (direct min-max). Normalize surface via `invert()` (hi - v)/span. compositeScore = `1.0 - max(normalized_overhang, normalized_footprint, normalized_cross, normalized_shadowed, inverted_surface, normalized_height)`. Sort descending.

For `rank_by_topsis`: vector-normalize each column (divide by sqrt of sum-of-squares, max 1e-9 floor). Apply weights. For surface, ideal-best = MAX (not min) and ideal-worst = MIN — pitfall #2 guard. The S+ contribution for surface is `(sBest - ws[i])²` and S- contribution is `(ws[i] - sWorst)²`. Skip columns where `w_i > 0.0` is false (no contribution). Sort descending by closeness. Return Vec.

For `merge_candidates`: convert `min_angle_deg` to `cos_threshold = (min_angle_deg * π/180).cos()` for dot-product comparison (cheaper than `acos`). Iterate the pre-sorted `scored: &[(usize, f32)]` slice. Maintain `picked: Vec&lt;[f32;3]&gt;`. For each candidate, compute `dot = dir·p` for each `p` in picked; if any `dot >= cos_threshold` skip (too close). Else push idx to result, dir to picked. Stop at max_candidates.

For `angle_between`: `let dot = (a·b) / (|a|·|b|); dot.clamp(-1.0, 1.0).acos() * 180.0 / PI`. Return 0.0 if either vector has zero length.

Run `cargo test --lib ranking selection` — all ground-truth tests MUST PASS (GREEN).
  </action>
  <verify>
    <automated>cd core &amp;&amp; cargo test --lib ranking 2&gt;&amp;1 | tail -3; cargo test --lib selection 2&gt;&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --lib ranking` report: `test result: ok` with all ranking tests passing.
    - `cargo test --lib selection` report: `test result: ok` with all selection tests passing.
    - `grep -v '^#' core/src/ranking.rs | grep -c 'unimplemented!()'` == 0 (no stubs remain).
    - `grep -v '^#' core/src/selection.rs | grep -c 'unimplemented!()'` == 0.
    - The ground-truth TOPSIS test asserts C2 closeness ≈ 0.739 within 0.01 (matches RESEARCH.md example hand-computation) — PASSES.
    - The surface-inversion test asserts C1 (higher surface) ranks before C0 with weights {w_surface:1.0}, all others 0 — PASSES (D-07 formula verified).
    - The diversity-filter test asserts merge_candidates returns exactly `[0, 2, 4]` for the 5-directions-at-15°-threshold case — PASSES.
    - `cargo test --lib` overall passes: existing tests still pass + new tests pass.
  </acceptance_criteria>
  <done>rank_by_weights, rank_by_consensus, rank_by_topsis, merge_candidates, angle_between all implemented in Rust; every ground-truth test passes (GREEN phase confirmed for ranking + selection).</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 4 (GREEN): Implement yaw.rs + deprecate old candidates.rs::compute_default_yaw</name>
  <read_first>
    - web/src/compute.ts lines 547-577 (quaternionAlign + multiplyQuats), lines 221-256 (computeDefaultYaw — the 180-sample bbox minimization algorithm)
    - web/src/compute.ts lines 600-602 (the full-quaternion convention: qFull = multiplyQuats(qYaw, qAlign) — STATE.md Phase 2 decision is LOCKED)
    - core/src/candidates.rs lines 66-94 (existing compute_default_yaw — DIFFERENT convention, now dead code per pitfall #6)
    - core/src/stability.rs (find_perpendicular helper pattern, if reusable for the perpendicular basis conversion in yaw)
    - core/src/mesh.rs (MeshData struct used by bbox_min_yaw)
    - .planning/phases/05-rust-consolidation/05-RESEARCH.md lines 521-605 (Example 1 yaw.rs — reference port)
  </read_first>
  <files>core/src/yaw.rs, core/src/candidates.rs</files>
  <behavior>
Per STATE.md Phase 2 LOCKED decision: full candidate quaternion = `qYaw * qAlign(dir, -Y)` — align dir to -Y first, then apply yaw. The Rust `yaw::full_quaternion` MUST produce a quaternion equivalent to the deleted TS `multiplyQuats(qYaw, qAlign)`.

`quaternion_align(a, b) -> [f32;4]` (port of TS `quaternionAlign` lines 547-565): both vectors assumed unit-length (caller normalizes). Returns `[w, x, y, z]` quaternion that rotates `a` onto `b`. Edge cases: `dot > 0.9999` → return identity `[1,0,0,0]`. `dot < -0.9999` → 180° rotation about a perpendicular axis: pick axis = cross(a, [1,0,0]) if |a.x| &lt; 0.9 else cross(a, [0,1,0]); normalize; return `[0, axisx, axisy, axisz]`. Otherwise: `axis = cross(a, b)`, normalize, `half = acos(dot)/2`, return `[cos(half), sin(half)*axisx, sin(half)*axisy, sin(half)*axisz]`.

`multiply_quats(a, b) -> [f32;4]` (port of TS `multiplyQuats` lines 567-577): Hamilton product `a * b` (applies b first, then a). Returns the standard 4-component product — copy the exact TS formula element-by-element.

`bbox_min_yaw(dir, mesh) -> [f32;4]` (port of TS `computeDefaultYaw` lines 221-256, renamed): the yaw-only quaternion (rotation by best_angle around `dn` axis). Steps: normalize dir → `dn`. Compute `up = -dn`. Build perpendicular basis: `upX = up × ([1,0,0] if |up.x|&lt;0.9 else [0,1,0])`, normalize; `upY = up × upX`. Project every vertex to 2D u-v: `u = v·upX`, `v = v·upY`. Build 2D convex hull (use existing `crate::stability::convex_hull_2d` or `crate::candidates::convex_hull_2d` — check which is public; if neither, copy a small inline hull helper). Brute-force 180 angles s in 0..180: `angle = s/180 × π`. Rotate each hull point by ±angle, compute bbox area = `(maxX-minX) × (maxY-minY)`. Track `best_angle` minimizing area. Return `[cos(best_angle/2), dn.x × sin(best_angle/2), dn.y × sin(best_angle/2), dn.z × sin(best_angle/2)]`.

`full_quaternion(dir, mesh) -> [f32;4]`: `let q_yaw = bbox_min_yaw(dir, mesh); let q_align = quaternion_align(dir, &[0.0,-1.0,0.0]); multiply_quats(&q_yaw, &q_align)`. Per the LOCKED TS convention.

The existing `core/src/candidates.rs::compute_default_yaw` uses a DIFFERENT convention (rotates +Z to dir, then yaw around dir, returns single composed quaternion). It must NOT be used by `score_all_directions` (Plan 02). Mark it `#[allow(dead_code)]` with a comment: `// DEPRECATED Phase 5 — replaced by yaw::full_quaternion. Different convention (Z-align, not -Y-align). Kept for the existing test.` Keep its existing `test_compute_default_yaw` test passing.
  </behavior>
  <action>
Implement the 4 yaw functions as faithful ports of the TS code per the &lt;behavior&gt; block. Use real (not stub) bodies. For the 2D convex hull inside `bbox_min_yaw`, prefer reusing existing Rust helpers: check if `crate::stability::convex_hull_2d` or `crate::candidates::convex_hull_2d` is exported `pub(crate)` — pick whichever is available (read both files to find out); if one exists, call it; if neither is accessible, write a small ~30-line Andrew's monotone-chain `convex_hull_2d` helper inside `yaw.rs` as `fn convex_hull_2d(points: &[[f32;2]]) -> Vec&lt;[f32;2]&gt;` (private, not `pub(crate)`). Do NOT depend on `crate::hull.rs` (that's a 3D hull).

For `multiply_quats`, copy the exact TS formula (lines 567-577) element by element — do not lookup a different convention. The 4 output components are:
```
[
  a[0]*b[0] - a[1]*b[1] - a[2]*b[2] - a[3]*b[3],
  a[0]*b[1] + a[1]*b[0] + a[2]*b[3] - a[3]*b[2],
  a[0]*b[2] - a[1]*b[3] + a[2]*b[0] + a[3]*b[1],
  a[0]*b[3] + a[1]*b[2] - a[2]*b[1] + a[3]*b[0],
]
```

For `quaternion_align`, copy the exact TS edge-case branches (identity when aligned, 180° about perpendicular when anti-aligned, half-angle axis otherwise). Use `f32::acos` and `f32::sin`/`f32::cos`.

For `bbox_min_yaw`, port the TS 180-sample loop literally. The output is `[cos(half), dn[0]*sin(half), dn[1]*sin(half), dn[2]*sin(half)]` where `half = best_angle/2`. Match TS exactly.

For `full_quaternion`, compose in the order `multiply_quats(&q_yaw, &q_align)` — `q_yaw * q_align` Hamilton product, NOT the reverse. Per Phase 2 LOCKED decision.

In `core/src/candidates.rs`, find the `compute_default_yaw` function (line 66). Add `#[allow(dead_code)]` directly above it (no other change). Add the comment `// DEPRECATED Phase 5 — replaced by yaw::full_quaternion. Different convention (Z-align, not -Y-align). Kept for the existing test.` immediately above the `#[allow(dead_code)]` line.

Run `cargo test --lib yaw` — every yaw ground-truth test MUST PASS. Run `cargo test --lib candidates` — the existing `test_compute_default_yaw` test MUST still pass. Run `cargo test --lib` — every test in the crate passes.
  </action>
  <verify>
    <automated>cd core &amp;&amp; cargo test --lib yaw 2&gt;&amp;1 | tail -3; cargo test --lib candidates 2&gt;&amp;1 | tail -3; cargo test --lib 2&gt;&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --lib yaw` reports `test result: ok` with all 8+ yaw tests passing.
    - `cargo test --lib candidates` reports `test result: ok` (existing test still passes).
    - `cargo test --lib` reports `test result: ok` overall — zero regressions.
    - `grep -v '^#' core/src/yaw.rs | grep -c 'unimplemented!()'` == 0 (no stubs remain in yaw.rs).
    - `grep -c 'DEPRECATED Phase 5' core/src/candidates.rs` == 1 (deprecated comment present).
    - `grep -B1 'pub(crate) fn compute_default_yaw' core/src/candidates.rs` shows `#[allow(dead_code)]` on the line immediately above (or in the comment block).
    - The `full_quaternion_unit_cube_dir_z_neg` test passes — asserts `[0.7071, 0.7071, 0, 0]` within 1e-3 (the hand-computed expected quaternion for a unit cube with dir=[0,0,-1]).
    - The `quaternion_align_z_to_neg_y` test passes — applies the result quaternion to [0,0,-1] and asserts the rotated vector is approximately [0,-1,0].
  </acceptance_criteria>
  <done>yaw.rs has 4 faithful ports (quaternion_align, multiply_quats, bbox_min_yaw, full_quaternion) matching the TS convention exactly; all yaw ground-truth tests pass; the deprecated candidates.rs::compute_default_yaw is marked dead-code with its own test preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust core ↔ WASM FFI | New ranking/selection/yaw functions are pure Rust — no FFI surface in this plan (WASM exports arrive in Plan 02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | f32 NaN/Inf in metric inputs | mitigate | All scoring functions already guard with `is_finite()` (existing pattern in scoring.rs); mirror in new rankers — skip NaN contributors in normalize loops (existing pattern) |
| T-05-SC | Tampering | No new package installs in this plan | accept | No cargo deps added — only features restructured. No slopcheck needed. |
</threat_model>

<verification>
After all 4 tasks complete:
- `cargo build` (default features = wasm) exits 0
- `cargo build --no-default-features` exits 0 (core compiles without wasm-bindgen — dual-target safety)
- `cargo test --lib` overall passes — existing tests still green, 20+ new ground-truth tests green
- `grep -c 'unimplemented!()' core/src/ranking.rs core/src/selection.rs core/src/yaw.rs` returns `0:0:0` (no stubs)
- `core/src/lib.rs` declares `mod ranking; mod selection; mod yaw;` (verify with `grep -n 'mod ranking\|mod selection\|mod yaw' core/src/lib.rs`)
- No TS files modified in this plan (TS deletion is Plan 03 — TS code remains the reference until then)
</verification>

<success_criteria>
- Three new Rust modules (`ranking.rs`, `selection.rs`, `yaw.rs`) exist with 20+ ground-truth tests, all passing
- Cargo.toml is dual-target-ready (wasm + cli features, optional deps)
- Faithful ports of rank_by_weights/consensus/topsis, merge_candidates, quaternion_align/multiply_quats/bbox_min_yaw/full_quaternion
- Surface quality formula inversion (D-07) verified by a dedicated ground-truth test
- Yaw convention matches the LOCKED TS qFull = qYaw × qAlign(dir, -Y), verified by a unit-cube ground-truth test
- Existing Rust tests still pass (zero regressions)
- `cargo test --lib` and `cargo build --no-default-features` both green
</success_criteria>

<output>
Create `.planning/phases/05-rust-consolidation/05-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

### New Rust modules
- `core/src/ranking.rs`:
  - `pub(crate) struct ScoreWeights { w_overhang, w_footprint, w_cross, w_surface, w_height }`
  - `pub(crate) struct CandidateMetrics { overhang, footprint, max_cross, surface, height, shadowed }`
  - `pub(crate) fn rank_by_weights(metrics, w) -> Vec<(usize, f32)>` — weighted-sum ranking (lower=better)
  - `pub(crate) fn rank_by_consensus(metrics) -> Vec<(usize, f32)>` — minimax consensus (higher=better, 6-term max incl. shadowed)
  - `pub(crate) fn rank_by_topsis(metrics, w) -> Vec<(usize, f32)>` — TOPSIS MCDA (higher=better)
- `core/src/selection.rs`:
  - `pub(crate) fn merge_candidates(scored, directions, stable_flags, exclude_unstable, max_candidates, min_angle_deg) -> Vec<usize>` — angle-diversity picker
  - `pub(crate) fn angle_between(a, b) -> f32` — degrees between two vectors
- `core/src/yaw.rs`:
  - `pub(crate) fn quaternion_align(a, b) -> [f32;4]` — rotation aligning unit vector a to unit vector b
  - `pub(crate) fn multiply_quats(a, b) -> [f32;4]` — Hamilton product a×b
  - `pub(crate) fn bbox_min_yaw(dir, mesh) -> [f32;4]` — yaw-only bbox-minimizing quaternion (port of TS computeDefaultYaw)
  - `pub(crate) fn full_quaternion(dir, mesh) -> [f32;4]` — full orientation quaternion = bbox_min_yaw × quaternion_align(dir, -Y)

### Modified files
- `core/Cargo.toml`: optional deps + `[features]` section (wasm + cli)
- `core/src/lib.rs`: `mod ranking; mod selection; mod yaw;` declarations
- `core/src/candidates.rs`: existing `compute_default_yaw` marked `#[allow(dead_code)]` with deprecation comment

### New tests (ground-truth)
- 5+ in `ranking.rs` (weighted-sum, consensus, TOPSIS, surface-inversion, empty)
- 5+ in `selection.rs` (diversity filter, unstable exclusion, max cap, angle_between orthogonal/parallel)
- 8+ in `yaw.rs` (quaternion_align identity/z-to-y/180°-axis, multiply_quats identity/inverse, bbox_min_yaw unit square, full_quaternion unit cube)