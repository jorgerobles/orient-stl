---
phase: 05-rust-consolidation
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified: []
autonomous: false
requirements: []
tags: [verification, parity, browser, cli, human-verify]

must_haves:
  truths:
    - "The CLI binary (Phase 5 success criterion #5) runs the full pipeline and outputs JSON without a browser"
    - "All metric tests in `cargo test --lib` are ground-truth (hand-computed), NOT self-referential (success criterion #6)"
    - "All TS metric/ranking test files are deleted (success criterion #7)"
    - "Browser UI produces identical results to CLI for the same STL + config (success criterion #8) — at least one ranker + profile combination verified by a human"
  artifacts:
    - path: ".planning/phases/05-rust-consolidation/05-04-VERIFICATION.md"
      provides: "Cross-verification record — CLI vs browser UI parity for 3 rankers"
      contains: "Parity"
  key_links:
    - from: "CLI JSON output (/tmp/opencode/orient-stl-cli-*.json from Plan 02)"
      to: "Browser UI candidate list (live DOM inspection)"
      via: "numeric comparison of quaternion + metrics + rank order"
      pattern: "quaternion|compositeScore"
---

<objective>
Cross-verify that the consolidated Rust pipeline produces identical results whether invoked through the CLI binary or the browser UI, for the same STL + critical-angle + profile + ranker combination. Per phase success criterion #8.

Purpose: Confirm the Phase 5 consolidation goal — "single source of truth in Rust" — actually delivers identical outputs across both consumers (CLI = independent verification, browser = production UI). A human-verify checkpoint validates the parity visually.
Output: A verification record at `.planning/phases/05-rust-consolidation/05-04-VERIFICATION.md` and a human sign-off.
</objective>

<execution_context>
@/home/jorge/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/jorge/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-rust-consolidation/05-CONTEXT.md
@.planning/phases/05-rust-consolidation/05-01-SUMMARY.md
@.planning/phases/05-rust-consolidation/05-02-SUMMARY.md
@.planning/phases/05-rust-consolidation/05-03-SUMMARY.md
@.planning/phases/05-rust-consolidation/05-03-AUDIT.md (TS no-duplicates audit)

# Reference inputs from Plan 02
# /tmp/opencode/orient-stl-cli-{weights,consensus,topsis}.json — CLI outputs for test-tetrahedron.stl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate CLI reference outputs for all 3 rankers + 2 profiles</name>
  <read_first>
    - .planning/phases/05-rust-consolidation/05-02-SUMMARY.md (CLI binary setup from Plan 02)
    - test-tetrahedron.stl (fixture path — repo root)
    - resources/Skulled_Wurm_Bird_WOBase.stl (larger fixture for a second parity point)
    - .planning/phases/05-rust-consolidation/05-CONTEXT.md lines 39-44 (CLI usage locked: `cargo run --bin cli -- input.stl --angle 30 --profile resin-biased --ranker topsis`)
  </read_first>
  <files>(no source files modified)</files>
  <action>
Generate the CLI reference matrix. Run the CLI binary on two STLs × three rankers × two profiles = 12 invocations:

Matrix:
- STLs: `test-tetrahedron.stl`, `resources/Skulled_Wurm_Bird_WOBase.stl`
- Rankers: `consensus`, `weights`, `topsis`
- Profiles: `resin-biased`, `equal`
- Common args: `--angle 30 --refine 0 --max-candidates 10`

For each invocation, capture stdout JSON to `/tmp/opencode/orient-stl-cli-<stl>-<ranker>-<profile>.json` (12 files). The CLI command pattern:
```
cargo run --bin cli --features cli -- <stl> --angle 30 --ranker <ranker> --profile <profile> --refine 0 --max-candidates 10 \
  > /tmp/opencode/orient-stl-cli-<stl-stem>-<ranker>-<profile>.json
```

For `test-tetrahedron.stl` with ranker=consensus, profile=resin-biased, the expected output is a JSON array of CliCandidate objects with fields: `rank`, `compositeScore`, `quaternion`, `dir`, `overhang`, `footprint`, `maxCross`, `surface`, `height`, `shadowed`, `stable`, `margin`, `contactArea`.

Validate each output is non-empty JSON (starts with `[`, ends with `]`). Validate the rank=0 entry exists for each. Capture the rank=0 candidate's `quaternion` + `compositeScore` from each of the 12 runs into a concise comparison table to be written to `.planning/phases/05-rust-consolidation/05-04-VERIFICATION.md`.
  </action>
  <verify>
    <automated>cd /home/jorge/Sandbox/orient-stl/core &amp;&amp; cargo run --bin cli --features cli -- ../test-tetrahedron.stl --angle 30 --ranker consensus --profile resin-biased 2&gt;/dev/null | head -8; ls -la /tmp/opencode/orient-stl-cli-*.json 2&gt;/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `cargo run --bin cli --features cli -- ../test-tetrahedron.stl --angle 30 --ranker consensus --profile resin-biased` exits 0 and outputs valid JSON starting with `[` and ending with `]`.
    - All 12 expected output files exist in `/tmp/opencode/`:
      `test-tetrahedron-consensus-resin-biased.json`, `test-tetrahedron-weights-resin-biased.json`, `test-tetrahedron-topsis-resin-biased.json`,
      `test-tetrahedron-consensus-equal.json`, `test-tetrahedron-weights-equal.json`, `test-tetrahedron-topsis-equal.json`,
      `Skulled_Wurm_Bird_WOBase-consensus-resin-biased.json`, ... `Skulled_Wurm_Bird_WOBase-topsis-equal.json`
      (verify `ls /tmp/opencode/orient-stl-cli-*.json | wc -l` == 12).
    - Each JSON file parses successfully (`python3 -c "import json; json.load(open('/tmp/opencode/orient-stl-cli-test-tetrahedron-consensus-resin-biased.json'))"` exits 0 — try this on all 12).
    - Each file contains a JSON array with at least 1 candidate (verify path `ls -la /tmp/opencode/` shows non-zero file sizes).
  </acceptance_criteria>
  <done>12 CLI reference JSON files exist in /tmp/opencode/, all valid JSON arrays with at least 1 candidate; ready for browser parity comparison.</done>
</task>

<task type="auto">
  <name>Task 2: Capture browser UI outputs via dev server + devtools scraping</name>
  <read_first>
    - .planning/phases/05-rust-consolidation/05-03-SUMMARY.md (the simplified worker + main.ts pipeline from Plan 03)
    - web/package.json (npm run dev → http://localhost:5173/)
    - .planning/phases/05-rust-consolidation/05-03-AUDIT.md (TS is rendering-only confirmed)
  </read_first>
  <files>(no source files modified — runs the dev server, captures DOM data)</files>
  <action>
Per success criterion #8: "Browser UI produces identical results to CLI for the same STL + config."

Start the Vite dev server in the background: `npm run dev — workdir: web` (this starts http://localhost:5173/). Wait for the "ready" line in stdout.

Use a headless browser automation to load `test-tetrahedron.stl` in the browser UI, set the ranker + profile via the dropdowns (existing UI), trigger the candidate computation, and scrape the resulting candidate list from the DOM. Two options:

Option A (preferred — no extra deps): write a small Node script `web/scripts/capture-parity.mjs` (one-shot, deleted at plan completion) that:
1. Uses Node 24's built-in `fetch` + the Vite dev server URL to load the page (but the UI uses Three.js + workers which need a real browser — fetch alone won't work).

Option B (single-shot curl + DOM inspection): since the candidate list is rendered into the DOM after compute, an actual browser is needed. Use `npx playwright` if available; otherwise the human-verify step (Task 3) handles the browser capture.

**Decision: skip headless browser scraping in this task (no Playwright installed, no extra npm deps to add — Phase 5 deferred anything not in scope). Move the browser UI capture INTO the human-verify checkpoint (Task 3).** Task 2 ends by confirming the dev server starts cleanly with the new codebase; the human (in Task 3) opens the browser and reads the candidate list.

Steps for Task 2:
1. Confirm `npm run build` succeeds (vite build) — the production browser bundle is built.
2. Start `npm run dev` in the background and capture the first 5 stdout lines to confirm "VITE ready" or equivalent.
3. Stop the dev server.
4. Write the verification scaffolding file `.planning/phases/05-rust-consolidation/05-04-VERIFICATION.md` with a template that Task 3's human will fill in:
   - A comparison table with 12 rows (matching the CLI matrix from Task 1) — columns: STL, ranker, profile, CLI rank=0 quaternion, CLI rank=0 composite, Browser rank=0 quaternion, Browser rank=0 composite, Parity (Y/N).
   - A "Steps to capture Browser column" section: open http://localhost:5173/, load the STL file, select the ranker + profile in the dropdowns, open DevTools Console, run a tiny JS snippet to extract `window.candidates.slice(0, 1)` (or whatever the global candidate store is — main.ts exposes it; if not, the human can read rank=0 from the candidate list DOM display).

For Task 2's automation, also confirm the audit-asserted assertions still hold by re-running:
- `grep -cE 'score_all_directions|rank_candidates|compute_norm_bounds|select_diverse' web/pkg/orient_core.d.ts` == 4
- `test ! -f web/src/compute.test.ts` && echo "compute.test.ts deleted"
- `cargo test --lib` exits 0
- `npx vitest run` exits 0 (4 test files)
- `npx tsc --noEmit` exits 0
- `npm run build` exits 0

Write these verification results as the "Automated pre-flight checks" section of `05-04-VERIFICATION.md`. Then prepare the template for Task 3's human fill-in.
  </action>
  <verify>
    <automated>cd /home/jorge/Sandbox/orient-stl/web &amp;&amp; npm run build 2&gt;&amp;1 | tail -3; cd ../core &amp;&amp; cargo test --lib 2&gt;&amp;1 | tail -3; cd ../web &amp;&amp; npx vitest run 2&gt;&amp;1 | tail -3; grep -cE 'score_all_directions|rank_candidates|compute_norm_bounds|select_diverse' web/pkg/orient_core.d.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 (production bundle builds cleanly with the consolidated pipeline).
    - `cargo test --lib` exits 0 (ground-truth tests, no self-referential tests per D-04).
    - `npx vitest run` exits 0 with exactly 4 test files (quaternion, rotate, convention, centering — per D-03).
    - `npx tsc --noEmit` exits 0.
    - `grep -cE 'score_all_directions|rank_candidates|compute_norm_bounds|select_diverse' web/pkg/orient_core.d.ts` == 4 (all 4 WASM exports reachable from JS).
    - `test ! -f web/src/compute.test.ts` passes.
    - `.planning/phases/05-rust-consolidation/05-04-VERIFICATION.md` exists with a 12-row comparison table template + automated pre-flight section filled in.
  </acceptance_criteria>
  <done>Production build green; all test + type-check gates pass; 4 WASM exports confirmed visible; verification template ready for human fill-in.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify browser UI produces identical results to CLI</name>
  <read_first>
    - .planning/phases/05-rust-consolidation/05-04-VERIFICATION.md (the 12-row template)
    - /tmp/opencode/orient-stl-cli-test-tetrahedron-consensus-resin-biased.json (CLI reference for at least one case)
    - web/src/main.ts (to know how to expose/read candidates from DevTools — `window.candidates` or similar)
  </read_first>
  <files>.planning/phases/05-rust-consolidation/05-04-VERIFICATION.md</files>
  <what-built>
Phase 5 consolidated every calculation into Rust:
- 4 new WASM exports: `score_all_directions`, `rank_candidates`, `select_diverse`, `compute_norm_bounds` (Plan 02 — Plan 02 yield gated behind `wasm` feature, verified via `web/pkg/orient_core.d.ts`).
- 3 new Rust modules: `ranking.rs` (rank_by_weights/consensus/topsis), `selection.rs` (merge_candidates + angle_between), `yaw.rs` (quaternion_align/multiply_quats/bbox_min_yaw/full_quaternion) — Plan 01, with ground-truth tests only.
- Native CLI binary `cli` (Plan 02, clap derive) — runs the full pipeline and outputs JSON without a browser.
- TS layer stripped: `compute.ts` is ~80 lines (types + decimateForScore + WEIGHT_PRESETS only); `compute.test.ts` deleted; 4 rendering-layer TS test files retained (quaternion, rotate, convention, centering); all metric/ranking/selection/yaw functions deleted from TS.
- Single thin worker dispatcher: postMessage to worker → worker calls `score_all_directions` + `rank_candidates` + `select_diverse` → posts `Candidate[]` back.
  </what-built>
  <how-to-verify>
Per success criterion #8: "Browser UI produces identical results to CLI for the same STL + config."

**Steps for the human verifier:**

1. Start the dev server in a terminal:
   ```
   cd /home/jorge/Sandbox/orient-stl/web
   npm run dev
   ```
   Wait for `VITE vX ready in Yms` line — confirm the URL is `http://localhost:5173/`.

2. Open the browser at `http://localhost:5173/`. Confirm the page loads with no console errors (open DevTools → Console; red text means a load issue — capture it in 05-04-VERIFICATION.md if so).

3. Load `test-tetrahedron.stl` via the file picker (drag-drop also works). Wait for the candidate list to populate (the progress bar should complete).

4. Set the **profile** dropdown to `resin-biased` and the **ranker** dropdown to `consensus` (the defaults). Wait ~1s for recompute if needed (or click the "Recalculate" button per the existing dirty/clean UI logic).

5. Open DevTools → Console and run:
   ```js
   JSON.stringify({ rank0: window.candidates?.[0] ?? document.querySelector('[data-candidate-rank="0"]'), count: (window.candidates ?? []).length })
   ```
   If `window.candidates` is undefined, inspect the DOM: scroll to the candidate list panel and read the rank-0 candidate's display (quaternion + compositeScore + dir are visible in the UI per VIEW-05).

6. Open the CLI reference file `/tmp/opencode/orient-stl-cli-test-tetrahedron-consensus-resin-biased.json` in a text editor. Read the `rank=0` entry's `quaternion` (4 floats), `compositeScore`, and `dir`.

7. Compare: the browser's rank-0 candidate must have the SAME quaternion (to within 1e-4 componentwise), SAME compositeScore (within 1e-3 of the CLI value), and SAME `dir` (within 1e-5) as the CLI's rank-0 entry.

8. Fill in the first row of the comparison table in `.planning/phases/05-rust-consolidation/05-04-VERIFICATION.md`:
   ```
   | test-tetrahedron | consensus | resin-biased | <CLI q> | <CLI score> | <Browser q> | <Browser score> | <Y/N> |
   ```

9. (Optional, strengthens verification) Repeat for the other 11 matrix cells by switching the ranker + profile dropdowns in the UI and reading each corresponding CLI JSON file. Even verifying just 1 cell confirms parity; verifying all 12 confirms across all rankers.

10. Optional parity hardening: load `resources/Skulled_Wurm_Bird_WOBase.stl` (larger mesh) and re-run ranker=consensus, profile=resin-biased. Compare to `/tmp/opencode/orient-stl-cli-Skulled_Wurm_Bird_WOBase-consensus-resin-biased.json`. If parity holds on a second mesh, confidence is higher.

**Expected outcomes:**
- Browser and CLI agree on rank=0 candidate orientation quaternions (within float epsilon).
- Browser and CLI agree on compositeScore (within 1e-3) — given the small numerical differences from f32 precision vs JS number (f64), some drift is expected; treat <1% as pass.
- The rank order matches: rank=0,1,2 in the browser list match the rank=0,1,2 entries of the CLI JSON.

**Failure handling:** if parity fails, capture the difference (browser quaternion + CLI quaternion + delta) into the Verification file. Common causes:
- Plan 03 worker bug: mis-decoding the N×13 float layout, swapping quaternion order, or wrong offset into `metrics[i*13+...]`.
- Plan 02 missing/incorrect export implementation.
- Convention mismatch in yaw::full_quaternion — go back to Plan 01's `full_quaternion_unit_cube_dir_z_neg` ground-truth test and check it actually asserts the rotated orientation matches the hand-computed expected.
  </how-to-verify>
  <resume-signal>Type "approved" if parity holds. If parity fails, paste the browser quaternion + CLI quaternion + delta into your response.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new code is written in this plan; verification only. Trust boundaries unchanged from Plan 03.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-V1 | Repudiation | Verification record incomplete | mitigate | The 12-row comparison table + automated pre-flight checks are written to `05-04-VERIFICATION.md` before human-verify; gaps are visible. |
| T-05-SC | Tampering | No package installs in this plan | accept | Verification only — no cargo/npm installs. |
</threat_model>

<verification>
After all 3 tasks complete:
- 12 CLI reference JSON files exist in `/tmp/opencode/` (all valid JSON arrays)
- `npm run build` exits 0
- `cargo test --lib` exits 0
- `npx vitest run` exits 0 with 4 test files
- `npx tsc --noEmit` exits 0
- 4 WASM exports visible in `web/pkg/orient_core.d.ts`
- `.planning/phases/05-rust-consolidation/05-04-VERIFICATION.md` exists with: automated pre-flight results + 12-row comparison table + human verification row(s) populated + final approval status
- Human has signed off "approved" OR a parity failure record is captured with deltas
</verification>

<success_criteria>
- All Phase 5 success criteria (1-8) hold at the end of this plan:
  1. Every scoring metric has exactly ONE implementation in Rust (no TS duplicate — confirmed by Plan 03 audit)
  2. Every ranking algorithm has exactly ONE implementation in Rust
  3. Candidate selection and yaw optimization run in Rust
  4. Single WASM `score_all_directions` replaces the worker-based `computeSlice` pipeline
  5. Rust CLI binary runs the full pipeline and outputs JSON (12 invocations succeed)
  6. All metric tests are ground-truth (Plan 01 + Plan 02 self-referential tests removed)
  7. All TS metric/ranking test files deleted; only rendering-layer tests remain (confirmed by Plan 03 audit + Task 2 pre-flight checks)
  8. Browser UI produces identical results to CLI for the same STL + config (human-verified in Task 3)
- A human has signed off "approved"
</success_criteria>

<output>
Create `.planning/phases/05-rust-consolidation/05-04-SUMMARY.md` when done (final phase verification complete + human approval captured)
</output>

## Artifacts this phase produces

### Reference inputs
- 12 JSON files at `/tmp/opencode/orient-stl-cli-<stl>-<ranker>-<profile>.json` — CLI outputs for `test-tetrahedron.stl` and `resources/Skulled_Wurm_Bird_WOBase.stl` across 3 rankers × 2 profiles

### Verification record
- `.planning/phases/05-rust-consolidation/05-04-VERIFICATION.md` — automated pre-flight checks + 12-row CLI↔browser comparison table + human sign-off section